# Review: Le jeu `hint-budget`, cadrer avant de demander, payer chaque indice

- **Verdict**: blocked
- **Diff**: `main...HEAD` (`b7e73ab`..`6afd03b`)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_30
- **Findings**: 5 critical, 10 warning, 5 minor

## Phases

### Phase 1 — Les contrats et la lecture pure des situations

- [x] Une situation dont toutes les lectures de cadrage sont établies est refusée au chargement, en nommant la situation — `src/games/hint-budget/schema/config.schema.ts:172-178`, test `__tests__/unit/games/hint-budget/config.schema.test.ts:159`
- [x] Une situation à zéro ou deux causes `actual` est refusée, en nommant la situation — `config.schema.ts:144-158`, tests `config.schema.test.ts:129,144`
- [x] Une configuration dont la surtaxe d'aveugle n'excède pas l'indice le plus cher est refusée, en nommant les deux montants — `config.schema.ts:181-187`, test `config.schema.test.ts:193`
- [x] Une trace qui omet une situation, tranche une cause inconnue, ou dont `afterHints` dépasse les achats, lève l'erreur nommée portant l'identifiant fautif — `schema/answer.schema.ts:189-229`, tests `answer.schema.test.ts:86,112,168`
- [x] Une trace qui achète deux fois le même indice dans une situation est refusée par le schéma — `answer.schema.ts:52-61`, test `answer.schema.test.ts:205`
- [x] Cadrage exact => `framingGrounded: true` ; une établie en moins ou une supposée en plus => `false` — `helpers/read-situations.helper.ts:58-66`, tests `read-situations.test.ts:108,121`
- [x] Cadrage exact posé après un achat => `framingGrounded: true`, `framedFirst: false`, `framedAndGrounded: false` — `read-situations.helper.ts:55-68`, test `read-situations.test.ts:91`
- [x] Une tranche fausse sans indice coûte strictement plus qu'une tranche fausse après l'indice le plus cher — `read-situations.helper.ts:80-83` + invariant `config.schema.ts:181`, test `read-situations.test.ts:177`
- [x] `lint`, `typecheck`, `test` passent — 201 fichiers Biome, 73 fichiers / 645 tests, revérifié par l'orchestrateur

### Phase 2 — L'évaluateur et ses deux règles

- [x] Acheter exactement la moitié des indices ne compte pas la situation comme frugale — inégalité stricte `hint-budget.evaluator.ts:57`, test `evaluator.test.ts:189`
- [x] Aucun achat et aucune bonne cause ne satisfait pas la frugalité — `evaluator.ts:55-58` exige `solved`, test `evaluator.test.ts:145`
- [x] Un cadrage exact posé après un achat ne compte pas pour le critère de cadrage — `read-situations.helper.ts:68` + `evaluator.ts:68-74`, test `evaluator.test.ts:155`
- [x] Un `rule.type` inconnu lève une erreur nommée citant le type et le jeu — `evaluator.ts:34-39,106`, test `evaluator.test.ts:226`
- [x] `lint`, `typecheck`, `test` passent — cf. phase 1

### Phase 3 — Le jeu à l'écran : cadrer, acheter, trancher

- [x] Deux parties aux mêmes gestes joués dans des ordres différents produisent une trace identique — `actions/build-hint-budget-answer.action.ts`, test `build-answer.test.ts:63,96`
- [x] Une fois le cadre déposé, aucune lecture ne change plus d'état — `use-hint-budget.hook.ts:99-106`, tests `use-hint-budget.test.ts:98`, `hint-budget-game.test.tsx:99`
- [x] Acheter puis déposer le cadre est accepté, la trace porte `afterHints` à 1 — `use-hint-budget.hook.ts:109-115`, test `use-hint-budget.test.ts:115`
- [x] Aucune manière dans l'API du hook d'acheter plus d'un indice par appel — `use-hint-budget.hook.ts:121-124` (un seul `hintId`), test `use-hint-budget.test.ts:156`
- [x] `advance()` deux fois à la dernière situation ne soumet qu'une trace — `use-hint-budget.hook.ts:161-165`, tests `use-hint-budget.test.ts:200`, `hint-budget-game.test.tsx:174`
- [x] Le rendu avant révélation ne contient ni `verification`, ni le texte d'un indice non acheté, ni rien qui distingue une lecture établie d'une supposition — `use-hint-budget.hook.ts:168-196`, `elements/framing-line.tsx:11-20`, tests `hint-budget-game.test.tsx:122,211` (comparaison d'arbres rendus, pas de signature de hook)
- [x] `lint`, `typecheck`, `test` passent — cf. phase 1

### Phase 4 — Le jeu dans le parcours, et son corpus

- [x] Ajouter le jeu n'a demandé que les deux blocs de câblage — `src/games/register-games.ts:68-72`, `src/games/register-components.ts`
- [x] Le parcours réel se charge sans refus, `g2-1` passe `hintBudgetConfigSchema` — test `hint-budget-run.test.ts:187`
- [x] `checkpoints-run` et `three-tracks-run` traversent les sept groupes sans refus — `__tests__/fixtures/hint-budget-answer.ts`, diffs des deux tests
- [x] Le cadreur frugal satisfait les deux critères — test `hint-budget-run.test.ts:207`
- [x] Le demandeur pressé manque les deux — test `hint-budget-run.test.ts:222`
- [x] Trancher systématiquement la première cause résout au plus une situation — test `hint-budget-run.test.ts:263` ; recalculé sur le corpus réel : rangs de la cause `actual` = 2, 4, 1, donc toute politique de rang fixe résout au plus 1/3
- [x] Retenir toutes les lectures ne satisfait le critère de cadrage dans aucune — test `hint-budget-run.test.ts:252` ; recalculé : 0/3
- [x] `lint`, `typecheck`, `test` passent — cf. phase 1

### Phase 5 — La passe impeccable de la surface

- [x] La fiche de surface existe et nomme la stratégie retenue — `.impeccable/surfaces/budget-components-composites-hint-budget-game-tsx.md` (mais cf. F15)
- [ ] Au gabarit mobile, le cadrage et le marché sont atteignables au même coût de geste — la parité est visuelle seulement. `hint-budget-game.tsx:78-107` place les deux panneaux comme frères d'une même rangée `grid grid-cols-2` : sommets et pieds identiques sont garantis par `align-items: stretch`, la mesure de `qa/README.md:34-39` est donc tautologique. Au clavier et au lecteur d'écran, le marché est à 6 arrêts de tabulation derrière le cadrage (5 `FramingLine` + « Transmettre ce cadre »), ce que `qa/README.md:41` concède sans le compter comme un écart
- [ ] Après cinq achats, l'action de trancher reste atteignable sans défilement aux deux gabarits — non atteint, et déjà non atteint **à l'ouverture** : 697px de dépassement desktop, 1346px mobile (`qa/README.md:58-59`). `phase-5.md` reste `status: done` sans consigner l'écart. Le renvoi au défaut transverse est inexact, cf. F5
- [x] Un test échoue si la présentation d'une lecture de cadrage dépend de sa nature — `hint-budget-game.test.tsx:211`
- [x] Un test échoue si la révélation qualifie le cadrage — `hint-budget-game.test.tsx:270`
- [x] `lint`, `typecheck`, `test` passent — cf. phase 1
- [x] La tournée aux deux gabarits est déposée dans `qa/`, mesurée à `scrollY = 0` vérifié — 12 captures présentes (mais cf. F4 : leur contenu ne montre pas ce que le README leur attribue)

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | fit | 4 | `config/course.json:491-522`, `591-622`, `691-722` | **La cause `actual` est le texte le plus long des cinq, dans les trois situations, avec une marge énorme** : s1 135 vs 62/52/52/44 · s2 137 vs 109/56/54/48 · s3 122 vs 79/74/64/51. Politique aveugle simulée sur le corpus réel : « trancher la cause la plus longue, n'acheter aucun indice, ne rien cadrer » => `c1` **TENU 3/3**, coût nul, zéro lecture. C'est le poids 2 de `pilotage-contexte` gagné sans jouer, ce que l'épique interdit nommément (« Un joueur qui tente de tricher un jeu n'obtient pas un cran supérieur »). Le jeu frère `lie-detector`, livré 5 commits plus tôt, verrouille exactement ce biais par deux tests (`__tests__/integration/course-run/lie-detector-run.test.ts:398` « never lets the liar be the longest or the shortest claim » et `:417` « keeps each round within a quarter of its longest claim ») ; `hint-budget` n'en porte aucun et son corpus les violerait tous les deux (ratio max/min = 3,07 sur s1) | Réécrire les cinq causes de chaque situation à longueur homogène — porter l'explication causale dans `verification`, pas dans `text`. Ajouter à `hint-budget-run.test.ts` les deux garde-fous de longueur déjà écrits chez `lie-detector` |
| 🔴 | fit | 4 | `config/course.json:484-489`, `584-589`, `684-689` | **L'indice le plus cher (`h5`, « Demander un diagnostic complet… ») énonce la cause réelle mot pour mot** dans les trois situations. `s1-h5` « Le serveur de production retarde son horloge : les jetons émis récemment lui paraissent invalides avant leur expiration réelle » ≈ `s1-c-clock:500`. Idem `s2-h5`/`s2-c-rounding`, `s3-h5`/`s3-c-parallel`. Or `c1` ne compte **que le nombre** d'indices : acheter le seul `h5` (1 sur 5 < 2,5) => `c1` **TENU 3/3**, simulé sur le corpus réel. La délégation totale — le comportement que l'épique cite comme triche à bloquer — est donc la stratégie optimale du jeu. Cela viole la règle d'écriture 4 de `phase-4.md:67` (« Aucun indice ne nomme une cause candidate »), elle-même contredite par la règle 2 de la même page (cf. F14) | Réécrire les cinq `h5` pour qu'ils écartent une piste ou apportent une mesure, jamais pour qu'ils nomment la cause. Sinon, faire lire au critère le coût engagé et pas seulement le compte |
| 🔴 | rot | 4 | `config/course.json:459-522` | **Le corpus de `s1` se contredit techniquement.** `s1-h3:476` « le serveur affiche une heure **en avance** de quatre minutes » ; `s1-h5:488` « le serveur **retarde** son horloge » — deux indices de la même situation affirment deux dérives opposées. `s1-h4:482` « rejeté pour une date d'émission jugée **future** » exige une horloge serveur **en retard**, tandis que `s1-c-clock:500` et sa `verification:502` (« invalides **avant leur expiration réelle** ») exigent une horloge **en avance**. `s1-h5` est en outre auto-contradictoire dans sa propre phrase. Enfin les nombres ne produisent pas la panne : rapport `:430` = jeton émis 4 min avant l'appel, fenêtre de 15 min ; une dérive de 4 min dans un sens ou l'autre laisse le jeton dans sa fenêtre. Le public évalué est composé de développeurs ; un incident faux discrédite la mesure, qui est la valeur centrale de l'épique (« on comprend pourquoi ») | Choisir une direction de dérive et l'appliquer aux quatre textes (`h3`, `h4`, `h5`, `c-clock` + sa `verification`), et porter l'écart au-delà de la fenêtre de validité |
| 🔴 | conform | 5 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/qa/` | **Les captures ne montrent pas ce que le README leur attribue.** Les 6 PNG mobiles sont des viewports 390×844 pris à `scrollY = 0`, or la grille cadrage/marché commence à y=902 d'après la mesure du README lui-même (`qa/README.md:38`) : `mobile-1`, `-2`, `-3`, `-4` n'en montrent donc **aucun pixel**. `qa/README.md:17` affirme pourtant « rapport, cadrage, marché tous visibles au premier rendu » — son propre artefact le réfute sur mobile. `mobile-3-s1-cinq-achats.png` est censé montrer « le relevé plafonné-et-replié » : il est pixel-identique à `mobile-1` hors la ligne « COÛT ENGAGÉ 75 ». `desktop-4-s1-revelation.png` montre les panneaux en phase `playing`, pas la révélation. La « septième capture » de la révélation en page pleine, décrite en prose à `qa/README.md:65` et servant de preuve unique au relevé « INDICES 0 · TRANCHE FAUSSE +40 · AVEUGLE +30 », **n'est pas dans le dépôt**. Seul `mobile-6-s3-desature.png` montre effectivement la grille à deux colonnes. CLAUDE.md : « Evidence over assertion » | Reprendre la tournée en captures pleine page (`fullPage`), ou corriger le tableau de `qa/README.md` pour ne décrire que ce que chaque fichier contient réellement, et déposer la capture de révélation |
| 🔴 | functional | 5 | `phase-5.md:129`, `qa/README.md:52-63` | **Critère d'acceptation non atteint et non consigné** : « Après cinq achats, l'action de trancher reste atteignable sans défilement aux deux gabarits ». Elle ne l'est déjà pas à l'ouverture (697px / 1346px de dépassement). `phase-5.md` porte `status: done` sans trace de l'écart, alors que la mémoire projet impose que les phases suivent le code. **Le renvoi au défaut transverse est inexact** : `aidd_docs/backlog/defects/la-revelation-pousse-l-action-hors-de-l-ecran.md` est délimité à l'état *révélation* (`:13`), à l'action de *passage* (`:15-16`), chez `lie-detector` et `defect-hunt` seuls, et son Impact repose explicitement sur « le verdict n'est pas faussé, aucun critère ne dépend de la position du bouton » (`:49`). Ici le défaut est à l'*ouverture*, porte sur *trancher* — le geste noté — et `hint-budget` n'est pas listé au ticket. Le ticket existe, mais il ne couvre pas ce cas : c'est un constat, pas un renvoi | Soit élargir le défaut existant et y inscrire `hint-budget` avec ses mesures, soit ouvrir un défaut propre à l'état d'ouverture ; dans les deux cas décocher le critère dans `phase-5.md` |
| 🟡 | functional | 5 | `hint-budget-game.tsx:78-107` | Critère « au même coût de geste » atteint visuellement, pas au clavier : le marché est 6 arrêts de tabulation derrière le cadrage. `c2` mesure précisément l'ordre choisi ; pour un joueur au clavier l'ordre favorable à `c2` est celui que la page impose | Mesurer et consigner l'écart de parcours clavier, ou poser les deux panneaux dans un ordre DOM qui ne privilégie ni l'un ni l'autre |
| 🟡 | rot | 4 | `config/course.json:744` vs `hint-budget.evaluator.ts:68-74` | **La question affichée au joueur sous-décrit sa règle.** `summary-view.tsx:96` rend `criterion.question` dans le verdict. `g2-1-c2` lit « Le contexte a-t-il été posé avant le premier indice ? » alors que `grounded-framings-at-least` exige *en plus* que le cadrage retienne exactement l'ensemble des lectures établies. Un joueur qui a cadré en premier mais imparfaitement se voit déclarer « manqué » sur un critère qui ne parle que d'ordre. Même écart sur `c1:729` : « L'incident a-t-il été résolu… » au singulier pour une règle à `threshold: 2` sur trois situations | Reformuler les deux `question` pour dire ce que la règle lit, sans annoncer les seuils |
| 🟡 | fit | 4 | `aidd_docs/backlog/stories/acheter-des-indices-a-contrecoeur.md:19` vs `plan.md:32` | **Arbitrage `c2` « fondé » : il tient sur le fond, il déborde la story sur la forme.** L'AC3 ne parle que d'ordre. Simulé sur le corpus réel : la politique « déposer un cadrage vide avant tout achat » donne `framedFirst` 3/3, donc `c2` TENU 3/3 sans lire une ligne — l'exigence de fondement est bien nécessaire au garde-fou imposé par l'épique. Mais elle n'a été portée ni dans la story, ni dans la question affichée (cf. F7) : le contrat d'acceptation dit encore autre chose que le code | Amender l'AC3 de la story pour y porter le fondement, et aligner la question de `c2` |
| 🟡 | code | 3 | `hint-budget-game.tsx:106`, `hint-card.tsx:33-39`, `framing-line.tsx:22-27` | **Affordances mortes après la tranche.** `HintMarket` ne reçoit pas `interactive` : après la révélation, chaque indice non acheté garde un bouton « ACHETER · n » d'apparence active dont le clic ne fait rien (`use-hint-budget.hook.ts:122` sort si `phase !== 'playing'`). Idem pour les `FramingLine` quand aucun cadre n'a été déposé : `locked={framingPosted}` reste `false` et les cases restent cliquables sans effet (`hook:100`). Contredit la docstring du hook `:70-72` (« le verrou tient par l'absence de chemin, jamais par une garde décorative ») : le chemin visible existe, il ne mène nulle part. Le test `hint-budget-game.test.tsx:158` ne couvre que les causes | Passer `interactive` à `HintMarket` et à `FramingLine`, et étendre le test « once revealed » aux deux inventaires |
| 🟡 | code | 5 | `hint-budget-game.tsx:78` vs `cut-panel.tsx:34` | **`grid grid-cols-2` sans point de rupture** : à 390px chaque colonne fait ~170px, ~150px de texte. `mobile-6-s3-desature.png` le montre — la lecture `s3-f1` se replie sur **7 lignes**, l'indice `s3-h2` sur 4, à ~14 caractères par ligne. Sur le même écran, `CutPanel` passe en `grid-cols-1` sous 640px pour ses causes : l'écran interdit une colonne unique au-dessus et l'impose en dessous. Incohérence interne, et la contrepartie n'est mesurée nulle part — `qa/README.md` mesure la parité (garantie par la grille) mais pas la lisibilité | Trancher : soit les causes passent aussi à deux colonnes sur mobile, soit la parité cadrage/marché se tient autrement qu'en écrasant les deux colonnes |
| 🟡 | conform | 4 | `config/course.json:418` | **Le label du jeu nomme la quantité notée** : « Combien d'indices vous faut-il ? ». `c1` note exactement le nombre d'indices achetés. `DESIGN.md:77` : « Un jeu ne dit jamais ce qu'il note. » `phase-4.md:94` a délibérément conservé le label du placeholder. Asymétrie : `c1` est télégraphié par le titre, `c2` ne l'est pas | Renommer sur l'incident, pas sur la ressource — le nom du type (`hint-budget`, choisi « par l'intention ») est mieux tenu que le label affiché |
| 🟡 | conform | 4 | `config/course.json:420` | **La consigne oriente sur une dimension notée** : « Les deux gestes se jouent dans l'ordre qui vous convient » alors que `c2` note cet ordre. Se taire est une chose ; rassurer faussement en est une autre. Décision assumée par `phase-5.md:47`, donc à rouvrir en cadrage plutôt qu'en code | Retirer la phrase : l'écran montre déjà les deux gestes côte à côte, l'absence d'ordre imposé n'a pas besoin d'être énoncée |
| 🟡 | fit | 2 | `hint-budget.evaluator.ts:100-109`, `read-situations.helper.ts:80-83` | **Le prix ne pèse sur rien.** Aucun critère ne lit un coût : `frugalSolvesAtLeast` compte `hintsBought`, `groundedFramingsAtLeast` compte des cadrages. Les prix `5·10·15·20·25`, `wrongCutPenalty` 40 et `blindCutSurcharge` 30 n'existent que dans la fiction et dans le pied de révélation. La décision du plan `:39` (« c'est l'écart de prix qui rend cadrer d'abord rentable ») n'a donc aucune contrepartie mesurée, et l'AC4 de la story ne survit que comme invariant de chargement. Effet combiné avec F2 : la stratégie la plus chère (acheter `h5` à 25) est aussi celle qui maximise `c1` | Décider : soit le coût entre dans une règle, soit le plan cesse de lui prêter un rôle d'arbitrage |
| 🟡 | rot | 4 | `phase-4.md:65` vs `phase-4.md:67` | **Deux règles d'écriture du corpus se contredisent** : règle 2 « Le plus cher désigne la cause » et règle 4 « Aucun indice ne nomme une cause candidate ». Le corpus a suivi la règle 2, ce qui a produit F2 | Supprimer l'une des deux ; la règle 4 est celle qui sert la mesure |
| 🟡 | rot | 5 | `.impeccable/surfaces/budget-…-hint-budget-game-tsx.md` (« Ce qui ne se négocie pas ») | **La fiche documente un mécanisme qui n'existe pas** : « la cause tranchée porte un anneau (`ring-inset`, hérité du motif de `lie-detector`) ». `grep -rn "ring" src/games/hint-budget/` ne rend rien ; `cause-option.tsx:36-61` marque la sélection par un `Disc` plein et le libellé « SÉLECTIONNÉE ». Docs contre code | Corriger la fiche sur le mécanisme réellement livré |
| 🟢 | code | 3 | `cause-option.tsx:33,49-55` | **Branche morte** : `selected` ne peut valoir `true` que lorsque `revealed` vaut `true`, puisque `cut-panel.tsx:43` le dérive de `revelation?.cutCauseId`. Le cas `selected && !revealed` — donc le `Disc` de la ligne 50 et le libellé `'sélectionnée'` — est inatteignable | Supprimer la branche, ou passer la cause sélectionnée pendant la partie si l'intention était de la montrer |
| 🟢 | code | 1 | `read-situations.helper.ts:40,136-139` | `Reading.totalCost` n'a aucun consommateur de production : ni l'évaluateur ni le hook ne le lisent, seul `read-situations.test.ts:207` l'assert. Exigé par `phase-1.md:115`, donc porté sans emploi | Retirer le champ, ou le brancher au `SpentMeter` qui recalcule aujourd'hui son propre cumul (`use-hint-budget.hook.ts:200-206`) |
| 🟢 | code | 3 | `hint-budget-game.tsx:174,189` | `onBuy` est passé aux `HintCard` déjà achetées, où `hint-card.tsx:27-31` ne l'appelle jamais. Prop inutile qui suggère une action inexistante | Ne passer `onBuy` que sur la branche non achetée |
| 🟢 | rot | 3 | `hint-budget-game.tsx:255` | **Collision de libellé** : la coquille affiche « SITUATION 1 SUR 20 » et le jeu « SITUATION 1 SUR 3 » à ~250px d'écart, avec des dénominateurs contradictoires (`desktop-1-s1-ouverture.png`). Les six autres jeux ont chacun choisi un nom distinct : « Manche » (`lie-detector-game.tsx:137`), « Tour » (`three-tracks-game.tsx:95`), « Étape » (`checkpoints-game.tsx:92`), « Extrait » (`confidence-bet-game.tsx:129`) | Nommer l'unité du jeu autrement que « situation », mot que la coquille possède déjà |
| 🟢 | code | 3 | `incident-brief.tsx:16` | `key={fact}` clé une liste sur son contenu ; `config.schema.ts:49` n'interdit pas deux faits identiques dans un `report` | Clé sur l'index, ou donner un `id` aux faits |

### Vérifié bon, à dire aussi

| Point | Preuve |
| --- | --- |
| **AC1** — coût annoncé, achat un par un, jamais en bloc | `hint-card.tsx:38` rend `Acheter · {cost}` sans survol ni dépliage (visible sur `desktop-1-s1-ouverture.png`) ; `use-hint-budget.hook.ts:121-124` n'expose qu'un `hintId` par appel et ignore un doublon ; aucune action groupée dans l'API. Tests `hint-budget-game.test.tsx:75,87` et `use-hint-budget.test.ts:156` |
| **AC2** — « moins de la moitié » ressort satisfait ou manqué | `hint-budget.evaluator.ts:57` applique `<` strict et non `<=`, verrouillé à la valeur pivot par `evaluator.test.ts:189` |
| **AC4** — la tranche aveugle et fausse coûte plus qu'un achat | Invariant de chargement `config.schema.ts:181-187` (`30 > 25`), plus une vérification exhaustive situation × indice sur le corpus réel, `hint-budget-run.test.ts:295` |
| **Aucune fuite dans le DOM** | Le hook ne construit jamais `established`, `actual`, `verification` ni le `text` d'un indice non acheté dans ses vues (`use-hint-budget.hook.ts:168-196`) ; `FramingLine` ne reçoit que `text`. Vérifié **dans le rendu**, pas dans la signature : `hint-budget-game.test.tsx:122` inspecte l'écran, `:211` compare deux arbres rendus HTML à l'octet près en ne changeant que l'identité de la lecture établie |
| **Aucune pénalité avant l'heure** | `use-hint-budget.hook.ts:239` : `spent` n'agrège que `currentHintCost` tant que la situation n'est pas révélée ; `cut-panel.tsx:56-61` n'affiche `wrongCutPenalty` et `blindCutSurcharge` que lorsqu'ils s'appliquent. Tests `use-hint-budget.test.ts:236`, `hint-budget-game.test.tsx:137` |
| **`DESIGN.md`, « un jeu ne dit jamais ce qu'il note » — sur la révélation** | `SituationRevelation` (`use-hint-budget.hook.ts:44-51`) ne porte aucun champ de cadrage ; test `hint-budget-game.test.tsx:270`. C'est le bon arbitrage, et il est mieux tenu que dans le `statement` et le label (F11, F12) |
| **Garde-fou `c2` contre les politiques aveugles** | Recalculé sur le corpus réel : « tout cocher » 0/3, « ne rien cocher » 0/3, « cocher au hasard » = 1/32 par situation soit ~0,3 % pour 2 sur 3. Le refus au chargement d'une situation sans lecture établie ou sans supposition (`config.schema.ts:160-178`) rend ces deux politiques impossibles à rendre gagnantes par un futur corpus |
| **Pas de trahison par la forme, côté cadrage** | Longueurs des lectures établies mêlées à celles des suppositions : s1 66/84 contre 92/81/75 · s2 87/55 contre 81/77/92 · s3 94/93 contre 95/87/67. Aucun indice de forme (contrairement aux causes, cf. F1) |
| **Les suppositions sont réellement non appuyées** | Les 9 vérifiées une par une contre leur `report` : aucune n'y est établie, et deux sont activement contredites (`s2-f3` « lignes en devise étrangère » contre le rapport `:528` « toutes dans la même devise ») |
| **Règle d'écriture 1 de `phase-4.md`** | Le rapport écarte exactement 2 causes sur 5 dans les trois situations : s1 `c-tls`+`c-secret`, s2 `c-currency`+`c-discount`, s3 `c-env`+`c-node` |
| **Gabarit structurel du parcours respecté** | Schéma de config + `parseHintBudgetTrace` + helper pur partagé écran/évaluateur + évaluateur à règles déclaratives + exactement deux blocs de câblage (`register-games.ts:68-72`, `register-components.ts`). Aucun contrat existant déplacé |
| **Trace sans champ dérivé** | `attemptSchema` (`answer.schema.ts:22-29`) ne porte que gestes et position ; `solved`, `framingGrounded`, `cost` se recalculent. `ForgedFramingError` (`:226-228`) refuse un `afterHints` impossible |

## Verification

| Metric | Value |
| ------ | ----- |
| Verified | 95% (35/37) |
| Files checked | `src/games/hint-budget/**` (11 fichiers), `src/games/register-games.ts`, `src/games/register-components.ts`, `config/course.json:410-757`, `__tests__/unit/games/hint-budget/**` (6), `__tests__/integration/course-run/hint-budget-run.test.ts`, `__tests__/fixtures/hint-budget-answer.ts`, `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/**` (plan + 5 phases + qa/), `.impeccable/surfaces/budget-…-hint-budget-game-tsx.md`, `src/features/scoring-summary/components/sections/summary-view.tsx`, `aidd_docs/backlog/defects/la-revelation-pousse-l-action-hors-de-l-ecran.md`, `DESIGN.md` |
| Unchecked | Phase 5 « Après cinq achats, l'action de trancher reste atteignable sans défilement » — fix · Phase 5 « Au gabarit mobile, cadrage et marché atteignables au même coût de geste » — fix |
| Unplanned | `cut-panel.tsx:28-34` et `cause-option.tsx:78-83` : abandon du motif `gap-px bg-plane-rule` de `lie-detector` au profit d'un filet par carte, correctif découvert pendant la tournée et non prévu au plan — justifié et documenté (`qa/README.md:50`), sans effet sur les autres jeux |
