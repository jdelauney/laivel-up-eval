# Review: Le jeu `hint-budget` — 5e tour

- **Verdict**: blocked
- **Diff**: `main...HEAD` (`b7e73ab`..`d86d389`, 20 commits)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_30
- **Findings**: 1 critical, 12 warning, 6 minor

## Phases

### Phase 1 — Les contrats et la lecture pure des situations

- [x] Une situation dont toutes les lectures de cadrage sont établies est refusée au chargement, en nommant la situation — `src/games/hint-budget/schema/config.schema.ts:258-263`
- [x] Une situation à zéro ou deux causes `actual` est refusée, en nommant la situation — `config.schema.ts:229-243`
- [x] Une configuration dont la surtaxe d'aveugle n'excède pas l'indice le plus cher est refusée, en nommant les deux montants — `config.schema.ts:450-456` (`30 > 25` sur le corpus réel)
- [x] Une trace qui omet une situation, tranche une cause inconnue, ou dont `afterHints` dépasse les achats, lève l'erreur nommée portant l'identifiant fautif — `answer.schema.ts:193,206,227`
- [x] Une trace qui achète deux fois le même indice dans une situation est refusée par le schéma — `answer.schema.ts:56-59`
- [x] Un cadrage retenant toutes les lectures établies et aucune supposition rend `framingGrounded: true`, `false` dès qu'on en retire ou ajoute une — `read-situations.helper.ts:83-91`
- [ ] Un cadrage exact posé après un achat rend `framingGrounded: true` et `framedFirst: false`, **donc `framedAndGrounded: false`** — le champ `framedAndGrounded` n'existe plus depuis la scission `c2`/`c3`. Les deux premiers tiers tiennent (`read-situations.helper.ts:78,88`), le troisième cite un champ supprimé. Signalé au tour 3, au tour 4, toujours ouvert (F1, m1)
- [x] Une tranche fausse sans indice coûte strictement plus qu'une tranche fausse après l'indice le plus cher — `config.schema.ts:450-456` + `hint-budget-run.test.ts:571`
- [x] `typecheck` / `test` passent — 665 tests, 73 fichiers ; `npx biome check src __tests__ config` propre sur 201 fichiers. `npm run lint` échoue au niveau dépôt via un worktree tiers de `.claude/worktrees/` portant son propre `biome.json` : condition préexistante, étrangère à cette branche

### Phase 2 — L'évaluateur et ses deux règles

- [x] Une trace qui achète exactement la moitié des indices ne fait pas compter la situation comme frugale — `hint-budget.evaluator.ts:56-59`, test `evaluator.test.ts:236`
- [x] Une trace sans achat et sans bonne cause ne satisfait pas la frugalité (`solved &&`) — `hint-budget.evaluator.ts:56-59`
- [x] Un cadrage exact posé après un achat ne compte pas pour le critère d'ordre — `read-situations.helper.ts:78-81`, test `evaluator.test.ts:178`
- [x] Un `rule.type` inconnu lève une erreur nommée citant le type et le jeu — `hint-budget.evaluator.ts:35-40,131`, test `evaluator.test.ts:273`
- [x] `typecheck` / `test` passent

### Phase 3 — Le jeu à l'écran : cadrer, acheter, trancher

- [x] Deux parties aux mêmes gestes dans des ordres différents produisent une trace identique — `build-hint-budget-answer.action.ts:29-35` (l'ordre suit la configuration, jamais le jeu)
- [x] Une fois le cadre déposé, aucune lecture ne change d'état — `use-hint-budget.hook.ts:99-106`, `framing-line.tsx` (`disabled={locked}`), test `hint-budget-game.test.tsx:107`
- [x] Acheter puis déposer le cadre est accepté, `afterHints` à 1 — `use-hint-budget.hook.ts:109-115`
- [x] Aucune manière d'acheter plus d'un indice par appel — `use-hint-budget.hook.ts:121-124`, `hint-card.tsx:31-49`, test `hint-budget-game.test.tsx:95`
- [x] `advance()` deux fois à la dernière situation ne soumet qu'une trace — `use-hint-budget.hook.ts:160-165`, test `hint-budget-game.test.tsx:240`
- [x] Le rendu avant révélation ne porte ni `verification`, ni le texte d'un indice non acheté, ni marque distinguant les deux natures de lecture — `use-hint-budget.hook.ts:168-196`, tests `hint-budget-game.test.tsx:130,277`
- [x] `typecheck` / `test` passent

### Phase 4 — Le jeu dans le parcours, et son corpus

- [x] Ajouter le jeu n'a demandé que les deux blocs de câblage — `register-games.ts`, `register-components.ts`
- [x] Le parcours réel se charge sans refus, et `g2-1` passe `hintBudgetConfigSchema` — `hint-budget-run.test.ts:202`
- [x] `checkpoints-run` et `three-tracks-run` traversent les sept groupes sans refus — `__tests__/fixtures/hint-budget-answer.ts`, branchée aux deux `:125` / `:122`
- [ ] Un profil qui cadre juste d'entrée et **résout deux situations sur trois** avec au plus deux indices satisfait **les deux** critères — faux sur les deux points depuis le tour 4 : `c1` exige 3 sur 3 (`config/course.json`, `g2-1-c1.rule.threshold: 3`) et le jeu porte trois critères. Le test censé le couvrir (`hint-budget-run.test.ts:222`) ne passe que par coïncidence de corpus (W3)
- [x] Un profil qui ne cadre jamais et achète tous les indices manque les trois — `hint-budget-run.test.ts:238`
- [x] Trancher systématiquement la première cause résout au plus une situation — `hint-budget-run.test.ts:315` (vérifié : seul `s3-c-parallel` est en tête de liste et réel)
- [x] Retenir toutes les lectures ne satisfait pas le critère de fondement — amendé par la scission `c2`/`c3` (`phase-4.md:200`), tenu sous sa forme courante : `c2` satisfait, `c3` manqué — `hint-budget-run.test.ts:278`
- [x] `typecheck` / `test` passent

### Phase 5 — La passe impeccable de la surface

- [x] La fiche de surface existe et nomme la stratégie retenue — `.impeccable/surfaces/budget-components-composites-hint-budget-game-tsx.md`
- [x] Mobile : l'écart de coût de geste est mesuré, nommé et assumé par écrit — `phase-5.md:139-152`, `qa/README.md` Points 1, 2 et 4 (marché en tête sur `s1`/`s3`, cadrage sur `s2` ; 5 puis 6 arrêts de tabulation)
- [x] Une lecture établie et une supposition sont rendues avec exactement la même structure — `framing-line.tsx` ne reçoit jamais `established`, test `hint-budget-game.test.tsx:277`, vérifié en DOM `qa/README.md` Point 6
- [ ] Après cinq achats, l'action de trancher reste atteignable sans défilement aux deux gabarits — non tenu, dès l'ouverture : 353,5px sous le pli desktop, 976,25px mobile (`qa/README.md` Point 3). Différé sur un défaut suivi, `aidd_docs/backlog/defects/l-ouverture-de-hint-budget-pousse-le-tranchage-hors-de-l-ecran.md`
- [x] Un test échoue si la présentation d'une lecture dépend de sa nature — `hint-budget-game.test.tsx:277`
- [x] Un test échoue si la révélation qualifie le cadrage — `hint-budget-game.test.tsx:336`
- [x] `typecheck` / `test` passent
- [x] La tournée aux deux gabarits est déposée dans `qa/`, mesurée à `scrollY = 0` vérifié — 18 captures, dimensions PNG recoupées une à une avec la table du README (aucun écart)

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | fit | 4 | `config/course.json` (`g2-1`, `situations[].framings` × `situations[].causes`) · aucun refus dans `src/games/hint-budget/schema/config.schema.ts` | **Le complément du panneau de cadrage donne la cause réelle, dans les trois situations.** Les deux lectures `established` de chaque situation correspondent une pour une aux deux causes `ruledOutByReport` (`s1-f1`→`s1-c-secret`, `s1-f2`→`s1-c-tls` ; `s2-f2`→`s2-c-currency`, `s2-f4`→`s2-c-discount` ; `s3-f3`→`s3-c-env`, `s3-f5`→`s3-c-node`) ; les suppositions reprennent les leurres restants (`s1-f3`→`s1-c-header`, `s1-f4`→`s1-c-cdn`, `s2-f5`→`s2-c-double` quasi mot pour mot, `s3-f1`/`s3-f4`→`s3-c-cache`). La cause réelle n'est reprise par **aucune** lecture ni **aucun** intitulé d'indice. Mesuré : la plus longue sous-chaîne commune entre une cause et les cinq lectures vaut 6 · 6 · 10 pour la cause réelle, minimum strict de sa situation (suivantes : 8 · 9 · 16). Scénario : lire le panneau « Le cadrage » et les intitulés du marché, barrer toute cause qu'ils nomment, trancher la survivante ⇒ `s1-c-clock`, `s2-c-rounding`, `s3-c-parallel`, 3 sur 3, zéro achat, rapport jamais lu, cadre jamais transmis ⇒ `c1` tenu, 2 des 4 points du jeu. Visible sur une seule capture : `qa/desktop-4-s1-revelation.png`, panneau de cadrage et panneau des causes côte à côte. C'est la fuite du tour 4 (complément des cibles d'indices) rejouée sur une seconde surface publique, que le plancher de deux causes ne couvre pas | Porter le garde-fou en contrat, comme au tour 2 : relier `framings` et `causes` dans `config.schema.ts`. Deux formes possibles — exiger qu'au moins une lecture reprenne la cause `actual` (le complément cesse d'être un singleton, symétrique du plancher de deux), ou interdire qu'une lecture soit mappable sur une cause (les lectures reformulent le rapport, jamais une hypothèse de panne). Puis réécrire le corpus en conséquence et mesurer le balayage combiné dans `hint-budget-run.test.ts` |
| 🟡 | rot | 1 | `src/games/hint-budget/schema/config.schema.ts:158-166` | Le docblock du schéma déclare encore les deux refus d'avant le tour 4 — « aucun indice **pris seul** ne peut […] ramener le champ en dessous de deux causes » et « un chemin frugal […] doit […] ramener le champ à **exactement une cause** » — alors que le code implémente « acheter **tous** les indices laisse au moins deux causes debout » (`:406-418`) et « une combinaison d'au plus la moitié des indices ramène au **plancher de deux** » (`:428-448`). Le commentaire inline `:387-388` dit lui-même que l'ancien refus a été retiré : le fichier se contredit à 250 lignes d'écart | Réécrire les deux puces du docblock sur les refus réellement posés |
| 🟡 | code | 4 | `__tests__/integration/course-run/hint-budget-run.test.ts` (aucune occurrence) · `config/course.json` (`g2-1-c1.rule.threshold: 3`) | **Le passage de `c1` à 3 sur 3 — la décision produit du tour 4 — n'est protégé par aucun test.** Aucun test ne lit le seuil, et chaque assertion sur `c1` a la même valeur de vérité à `2` et à `3` : `frugalFramer` → 3 résolutions frugales (vrai des deux côtés), `eagerAsker`/`spendthrift` → 0, `emptyFramer` → 1. Ramener `threshold` à `2` laisse les 665 tests verts | Ajouter un profil qui résout frugalement exactement deux situations sur trois et attendre `c1` manqué — le seul test qui distingue les deux seuils |
| 🟡 | code | 4 | `__tests__/integration/course-run/hint-budget-run.test.ts:110-134,222` | Le test de tête (`satisfies all three criteria…`) ne passe que par coïncidence, et son commentaire est faux. Il annonce « La troisième situation ne pèse sur aucun des deux seuils (`threshold: 2`) » et joue `s3` sans cadrage, sans achat, en tranchant `causes[0]`. `c1` exige 3 sur 3 : le test ne tient que parce que `s3.causes[0]` **est** `s3-c-parallel`, la cause réelle. Réordonner les causes de `s3` casse le test de tête pour une raison sans rapport avec ce qu'il prétend démontrer | Faire jouer les trois situations correctement au profil, corriger le commentaire (`threshold: 3`, trois critères), et laisser le profil de W2 porter la démonstration du seuil |
| 🟡 | fit | 4 | `config/course.json` : `s1-c-cdn.verification`, `s2-c-double.verification`, `s3-c-cache.verification` | **La révélation écarte les trois dernières causes par un fait que le joueur ne peut plus obtenir.** `69633bd` a supprimé les trois indices qui portaient cette preuve (`s1-h4` « La réponse est marquée non-cacheable et n'entre jamais dans le cache CDN », `s2-h2` « Le total n'est calculé qu'une seule fois, côté serveur », `s3-h3` « Le cache est reconstruit à l'identique ») sans toucher les `verification` correspondantes. Les douze autres causes citent le rapport ou un indice achetable mot pour mot ; seules ces trois — précisément les survivantes que `phase-4.md:219-223` dit départagées par le rapport — citent un fait absent de l'écran. Un joueur qui raisonne juste depuis la ligne prévue ne le voit jamais confirmé ; un joueur qui se trompe reçoit un fait qu'il n'avait pas. Visible sur `qa/desktop-4-s1-revelation.png` et `qa/desktop-5b-s2-revelation.png` | Réécrire les trois `verification` sur la ligne de rapport que `phase-4.md:221-223` désigne (« un cache resservirait la même réponse », « un double calcul frapperait toutes les lignes », « un cache corrompu échouerait de façon déterministe ») |
| 🟡 | rot | 4 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/phase-4.md:101,107,191` | Le contrat publié de `c1` est faux sur trois points : `threshold: 2` (la configuration porte `3`), la question citée entre guillemets « L'incident a-t-il été résolu… » n'existe pas dans `config/course.json` (« Les incidents ont-ils été résolus… »), et « La marge d'une situation laisse un lecteur qui se trompe une fois satisfaire le critère » (`:107`) contredit frontalement « `c1` n'a plus de marge d'erreur » (`:148`) | Reprendre la table des critères et le paragraphe de seuil sur la configuration livrée ; citer la question mot pour mot |
| 🟡 | rot | 4 | `phase-4.md:139-152` | La section « Trois chances » commente encore l'ancien tableau : elle porte quatre lignes sous un titre qui en annonce trois ; `:150` (« La troisième ligne […] déduire la survivante exige de lire les éliminations des **deux indices achetés** ») décrit la quatrième ligne, la troisième n'achetant rien ; `:152` (« La deuxième ligne […] 25,9 % dépasse les 15,6 % ») nomme une valeur qui est aujourd'hui la colonne « tour 3 » de la troisième ligne, et contredit `:148` (« Les quatre politiques passent désormais sous les 15,6 % ») ; le paragraphe se clôt sur un conditionnel (« relever le seuil de `c1` à 3 sur 3 le ramènerait à 3,7 % ») pour une décision déjà livrée | Réécrire les deux paragraphes sur le tableau courant, ou les supprimer : le tableau se suffit |
| 🟡 | rot | 4 | `phase-4.md:89,150` · `__tests__/integration/course-run/hint-budget-run.test.ts:437-441,475-477` | La « paire utile » n'existe plus et ses positions sont fausses. Avec une élimination par indice et une seule cause en lice visée par situation, l'indice utile est unique : position 1 sur `s1`, 5 sur `s2`, 4 sur `s3` — pas `{1,4}`, `{2,5}`, `{3,4}`. « Aucune paire de positions fixe n'est utile plus d'une fois » est faux : la paire fixe `{1,4}` atteint le plancher sur `s1` **et** `s3`. Le docstring de `hint-budget-run.test.ts:437-441` va plus loin et contredit le refus que son propre test vérifie : « `h1` et `h2` de chaque situation écartent chacun l'une des deux causes que le rapport laisse en jeu […] ramène le champ à la seule cause réelle » — impossible depuis le plancher de deux | Réécrire les trois passages sur l'économie du tour 4. La conclusion tient encore, par une autre raison : les trois positions utiles (1, 5, 4) étant distinctes, aucune paire fixe n'atteint le plancher sur les trois situations |
| 🟡 | rot | - | `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/plan.md:20,45-48` | La table des décisions s'arrête au tour 2 : ni la cardinalité exacte de `eliminates` (tour 3), ni le plancher de deux causes avec le passage de `c1` à 3 sur 3 (tour 4) n'y figurent — la décision produit la plus lourde de la branche ne vit que dans `phase-1.md` et `phase-4.md`. `:20` intitule encore la phase 2 « L'évaluateur et ses deux règles » pour trois règles | Ajouter les deux décisions manquantes ; renommer la ligne de phase 2 |
| 🟡 | fit | 4 | `plan.md` (décision « Les indices sont prix croissant à mesure qu'ils tranchent ») · `config/course.json` | Le marché d'indices n'a plus d'arbitrage, et la décision de plan qui le justifie est fausse sur le corpus. Depuis le plancher, aucun indice n'est décisif par construction, et le seul indice qui écarte une cause encore en lice coûte `5` sur `s1`, `25` sur `s2`, `20` sur `s3` : le prix ne suit plus ce que l'indice tranche. Pire, sur `s2` et `s3` la ligne discriminante du rapport écarte déjà le dernier leurre en lice (un taux de TVA faux ne épargnerait pas les quantités entières ; un fuseau horaire différent échouerait de façon déterministe), donc un lecteur compétent n'achète rien et paie zéro. « Résolu en achetant moins de la moitié des indices » dégénère en « n'a pas trop acheté » | Trancher explicitement : soit assumer par écrit que le marché est un piège à mesurer et retirer la décision de prix croissant du plan, soit rendre un achat réellement nécessaire (une cause en lice qu'aucune ligne du rapport n'implique) |
| 🟡 | functional | 4 | `phase-4.md:191` | Critère d'acceptation périmé : « Un profil qui cadre juste d'entrée et résout **deux situations sur trois** avec au plus deux indices satisfait **les deux** critères » — `c1` exige 3 sur 3 et le jeu porte trois critères | Réécrire le critère sur la configuration livrée (cf. W5) |
| 🟡 | functional | 1 | `phase-1.md:135` | Critère d'acceptation citant un champ supprimé : « … donc `framedAndGrounded: false` ». `SituationReading` ne porte plus ce champ depuis la scission `c2`/`c3` | Retirer le troisième membre du critère (cf. m1) |
| 🟡 | functional | 5 | `phase-5.md` (ligne 2 de la table) · `qa/README.md` Point 3 | Critère non tenu : trancher est sous le pli dès l'ouverture, aux deux gabarits (353,5px desktop, 976,25px mobile). Différé par décision produit sur un défaut suivi | Hors périmètre de cette branche selon `phase-5.md:170-174` ; laissé au défaut `l-ouverture-de-hint-budget-pousse-le-tranchage-hors-de-l-ecran.md` |
| 🟢 | rot | 1 | `phase-1.md:104,110,115` | `SituationReading` y est encore exporté avec `framedAndGrounded` (« les deux à la fois, le seul cas qui vaut quelque chose au scoring »), et `Reading` décrit sans `framedFirstCount`, avec `groundedFramingCount` défini comme « le compte de `framedAndGrounded` ». Le code compte `framingGrounded` seul (`read-situations.helper.ts:143-146`). Signalé aux tours 3 et 4. Contrairement à `phase-2.md:7`, `phase-1.md` ne porte aucune mention d'historique qui excuserait l'écart | Corriger les trois lignes, ou poser en tête la même clause d'historique que `phase-2.md` |
| 🟢 | rot | 1 | `phase-1.md` (tâche 2, point 6) | « Poser **cinq** erreurs nommées » suivi de six erreurs listées ; la table d'acceptation de la même page dit « les **six** erreurs nommées » | Corriger le compte |
| 🟢 | rot | 1 | `phase-1.md:14` | Le commentaire de l'arbre annonce « ses neuf refus » ; le schéma en porte seize (neuf + sept ajoutés au tour 2) | Corriger le compte, le bloc de correction plus bas donne déjà le bon |
| 🟢 | code | 4 | `__tests__/integration/course-run/hint-budget-run.test.ts:414` | Le test « never lets a single hint […] narrow a real situation down to one cause » est strictement subsumé par celui de `:442` (si tous les indices laissent ≥2 causes, tout sous-ensemble aussi) et vérifie un refus que le schéma ne porte plus depuis `69633bd` | Supprimer le test, ou le fondre dans celui du plancher |
| 🟢 | code | 1 | `src/games/hint-budget/schema/config.schema.ts:337` | `if (targetId === undefined …)` est inatteignable : `eliminates` est `z.array(...).length(1)` et un échec du schéma de base court-circuite le `superRefine` | Retirer la garde, ou justifier en commentaire si `noUncheckedIndexedAccess` l'impose |
| 🟢 | code | 1 | `src/games/hint-budget/schema/config.schema.ts:336,357` | Le même tableau à un élément est lu deux fois de deux façons : `const [targetId] = hint.eliminates` puis `hint.eliminates.some(...)` | Uniformiser sur la déstructuration |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 89% (33/37)                                        |
| Files checked | `config/course.json`, `src/games/hint-budget/schema/config.schema.ts`, `src/games/hint-budget/schema/answer.schema.ts`, `src/games/hint-budget/helpers/read-situations.helper.ts`, `src/games/hint-budget/hint-budget.evaluator.ts`, `src/games/hint-budget/hooks/use-hint-budget.hook.ts`, `src/games/hint-budget/actions/build-hint-budget-answer.action.ts`, `src/games/hint-budget/components/composites/hint-budget-game.tsx`, `src/games/hint-budget/components/composites/cut-panel.tsx`, `src/games/hint-budget/components/composites/incident-brief.tsx`, `src/games/hint-budget/components/elements/hint-card.tsx`, `src/games/hint-budget/components/elements/framing-line.tsx`, `src/games/register-games.ts`, `src/games/register-components.ts`, `__tests__/integration/course-run/hint-budget-run.test.ts`, `__tests__/unit/games/hint-budget/*`, `__tests__/fixtures/hint-budget-answer.ts`, `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/plan.md`, `phase-1.md`, `phase-2.md`, `phase-3.md`, `phase-4.md`, `phase-5.md`, `qa/README.md`, les 18 captures de `qa/`, `aidd_docs/backlog/stories/acheter-des-indices-a-contrecoeur.md`, `DESIGN.md` |
| Unchecked     | Phase 1 « … donc `framedAndGrounded: false` » — fix · Phase 4 « résout deux situations sur trois […] satisfait les deux critères » — fix · Phase 5 « après cinq achats, trancher reste atteignable sans défilement » — fix (différé, défaut suivi) · Phase 4 « retenir toutes les lectures ne satisfait le critère de cadrage dans aucune » — fixed (amendé par la scission `c2`/`c3`, `phase-4.md:200`) |
| Unplanned     | none |
