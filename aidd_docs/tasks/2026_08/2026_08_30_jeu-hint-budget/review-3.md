# Review: Le jeu `hint-budget`, troisième tour

- **Verdict**: blocked
- **Diff**: `b7e73ab...604ece2` (`main...HEAD`, 16 commits), réparations `6e9ade6..0e06e1f` puis `e8e591b..604ece2`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_30
- **Findings**: 1 critical, 5 warning, 6 minor

Troisième checker frais. Les deux canaux que le tour 2 nommait (`h5` éliminateur exhaustif, cadrage vide) sont **réellement fermés**, en contrat et pas en consigne. Le canal qui reste est **le même que celui du tour 1 (F2, la paraphrase), déplacé d'un indice et jamais rouvert** : c'est le commit de réparation du tour 1 lui-même (`3308dc0`) qui l'a créé, en posant son garde-fou sur la position qu'il venait de vider.

Toutes les mesures ci-dessous sont recalculées par le checker sur `config/course.json` livré. Tests rejoués : `npx vitest run __tests__/unit/games/hint-budget __tests__/integration/course-run/hint-budget-run.test.ts` → 8 fichiers, 92 tests, 0 échec. Les 16 PNG de `qa/` sont relus à l'octet et trois d'entre eux à l'image.

## Phases

### Phase 1 — Contrats et lecture pure

- [x] Le graphe d'élimination est en contrat, pas en consigne — `config.schema.ts:241-368`, cinq refus, chacun testé sur son propre cas dans `config.schema.test.ts`
- [x] `framedFirst` a un plancher de contenu : `retainedIds.length > 0` — `read-situations.helper.ts:78-81`. Le profil qui déclenchait C2 du tour 2 est couvert et ne tient plus rien — `hint-budget-run.test.ts:299-313` (W9 clos)
- [x] La cause `actual` n'est jamais `ruledOutByReport` ni dans un `eliminates` — `config.schema.ts:269-299`, vérifié sur le corpus réel
- [x] Aucun indice seul ne ramène le champ sous 2 causes, un chemin frugal existe partout — `config.schema.ts:315-368`, recalculé : 3 causes en lice après chaque indice de chaque situation
- [ ] **Le refus « les indices n'écartent des alternatives, jamais ne confirment ou ne nomment la bonne réponse » (`phase-1.md`, amendement §2) n'existe pas** : le schéma ne borne que le champ `eliminates`, jamais le texte. Cf. C1

### Phase 2 — L'évaluateur

- [x] Trois règles déclaratives, chacune sur un seul axe de lecture — `hint-budget.evaluator.ts:51-95`, `VerdictInputs:137-141`
- [x] `UnknownRuleError` nomme le type et le jeu — `:35-40`
- [x] L'inégalité de frugalité est stricte, et exige résolu ET frugal — `:56-59`
- [x] Un cadrage exact posé après un achat manque `c2`, tient `c3` — `evaluator.test.ts`, cas dédié

### Phase 3 — Le jeu à l'écran

- [x] Le hook n'expose ni `established`, ni `actual`, ni `verification`, ni le texte d'un indice non acheté avant l'heure — `use-hint-budget.hook.ts:168-196`
- [x] L'achat est unitaire et non répétable — `:121-124`, aucune action groupée dans l'API
- [x] `advance()` deux fois ne soumet qu'une trace — `:161-165`, `submittedRef`
- [x] La cause tranchée se distingue des causes écartées par une quantité — `cause-option.tsx:39-72`, `cut-panel.tsx:46-48`, vérifié au pixel sur `qa/desktop-4-s1-revelation.png` (« ÉCARTÉE — TRANCHÉE ») (W6 du tour 2 clos)
- [x] `CauseOption.selected` et `aria-pressed` mort supprimés — `cause-option.tsx:27-33` (W5c clos)
- [ ] **La docstring du composant décrit une consigne qui n'existe plus** — `hint-budget-game.tsx:32`. Cf. W2

### Phase 4 — Le corpus et les critères

- [x] Le rapport écarte exactement 2 causes sur 5 dans les trois situations — recalculé cause par cause
- [x] Rangs déclarés de la cause réelle : 1 · 3 · 0, distincts — testé `:324-332`
- [x] Rangs de longueur descendants : 2 · 3 · 4, distincts ; balayage k = 1..5 → 0/3, 1/3, 1/3, 1/3, 0/3 — testé `:381-405` (W7 du tour 2 clos, et le nouveau test échouerait sur l'ancien corpus, ce qui est la bonne façon de le fermer)
- [x] La question de `c1` est passée au pluriel — `course.json:730`
- [x] AC4 de la story tenue et outillée : surtaxe d'aveugle (30) > indice le plus cher (25), refus au chargement `config.schema.ts:371-377`, plus une vérification par situation × par indice `:524-561`. Relevé observé en tournée : `0 + 40 + 30 = 70` contre `25 + 40 = 65`
- [ ] **Règle 4 (`phase-4.md:67`) — « un indice peut écarter des causes par leur nom, jamais confirmer ou paraphraser celle qui reste » — est enfreinte dans les trois situations.** Cf. C1
- [ ] **`phase-4.md:112` (« le chemin frugal qui reste, `h1` + `h2` ») ne connaît pas le chemin à un seul indice qui existe dans les trois situations.** Cf. C1

### Phase 5 — La passe de surface

- [x] Le sens de l'alternance est inversé et mesuré dans le bon sens — `hint-budget-game.tsx:123`, corroboré à l'image : `desktop-4`/`mobile-6` (L'ASSISTANT en tête), `desktop-5` (LE CADRAGE en tête) (W1 du tour 2 clos ; l'affirmation « élimine le biais systématique » est retirée des trois documents et remplacée par un énoncé exact)
- [x] La fiche de surface porte les mesures courantes : −119px mobile, « trois choses distinctes », `wasCut` — `.impeccable/…:38,22` (W4 et W5a-b du tour 2 clos)
- [x] La tournée est rejouée en entier, `s2` comprise, 16 captures, `scrollY = 0` vérifié — dimensions PNG relues à l'octet, conformes à `qa/README.md:34` sans exception (1440×1510 · 1440×1473 · 390×2280 · 390×2161 ; viewport 1440×900 · 390×844)
- [x] Le renvoi normatif fabriqué du tour 2 est corrigé : le défaut cite `DESIGN.md:74` mot pour mot et dit franchement qu'aucune règle ne couvre son cas — `defects/l-ouverture-…:24`, vérifié contre `DESIGN.md:74` (C3 du tour 2 clos)
- [ ] « Après cinq achats, trancher reste atteignable sans défilement » — toujours non tenu, assumé par écrit, défaut ouvert (inchangé, non compté comme régression)
- [ ] **Le défaut ouvert porte des mesures que la tournée qu'il cite comme unique preuve contredit.** Cf. W3

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | fit | 4 | `config/course.json:478`, `:595`, `:705` · `config.schema.ts:126-129` · `hint-budget-run.test.ts:474-509` | **C1 — Le canal qui reste : l'indice qui *confirme* la cause au lieu de l'écarter. `c1` (poids 2) est tenu 3/3 en achetant un seul indice sur cinq, sans lire une ligne du rapport ni cadrer.** Le contrat ajouté au tour 2 ne borne que le champ `eliminates` ; il ne dit rien du texte. Les six indices à `eliminates: []` (`h3` et `h4` des trois situations) sont précisément ceux qui portent la matière, et trois d'entre eux tranchent la situation à eux seuls. Preuve la plus dure, plus longue sous-chaîne commune entre le texte de l'indice et (texte + `verification` de la cause réelle), calculée par le checker : `s1-h3` (15 pièces) = **80 caractères**, mot pour mot — « serveur de production accuse vingt minutes de retard sur la source de référence ». Le garde-fou du projet lui-même fixe la limite à **20** (`hint-budget-run.test.ts:476`), mais ne l'applique qu'à l'indice **le plus cher**. **Provenance, reconstituée commit par commit** : à `0d3ae27`, `s1-h3` valait 23 et `s1-h5` valait 40 ; c'est `3308dc0` — le commit de réparation du tour 1, celui qui *ajoute* le garde-fou LCS — qui a fait tomber `h5` à 7 et **monter `h3` à 80**. La paraphrase que le tour 1 sanctionnait n'a pas été supprimée : elle a été doublée et déplacée d'un cran, et le garde-fou a été posé sur la position qu'elle venait de quitter. Deux tours ne l'ont pas vue parce que tous deux ont mesuré `h5`. **Scénario, joué sur le corpus réel, sans rien lire d'autre que l'indice acheté** : `s1` acheter `s1-h3` (15) ⇒ « le serveur de production accuse vingt minutes de retard » ⇒ une seule cause parle d'horloge et de retard, `s1-c-clock`, réelle ⇒ résolue, 1 indice ; `s2` acheter `s2-h3` (15) ⇒ « le total sans arrondi intermédiaire diffère » ⇒ une seule cause parle d'arrondi, `s2-c-rounding`, réelle ⇒ résolue, 1 indice ; `s3` acheter `s3-h4` (20) ⇒ « l'agent CI exécute les tests sur plusieurs threads à la fois » ⇒ une seule cause parle de parallèle, `s3-c-parallel`, réelle ⇒ résolue, 1 indice. `1 < 5 × 0,5` partout ⇒ **`c1` TENU 3/3**, aucun rapport lu, aucun cadrage, aucun raisonnement d'élimination — un appariement de mot-clé entre une phrase achetée et cinq libellés. Le refus « aucun indice seul ne ramène le champ sous deux causes » (`config.schema.ts:319-338`) voit `remainingAfterHint = 3` pour ces trois indices, parce que leur `eliminates` est vide : **le contrat déclaré dit qu'ils n'écartent rien, leur texte tranche la situation.** C'est la délégation totale que l'épique nomme comme triche à bloquer (`parcours-couvrant-les-axes.md:32`) et le comportement que la story dit mesurer *contre* (« si je demande avant de réfléchir ») | Étendre le garde-fou de recouvrement à **tous** les indices, pas au plus cher, et le mesurer contre `text` **et** `verification` de la cause réelle : c'est l'instrument qui existe déjà et qui échoue de 4× sur `s1-h3`. Puis réécrire les six indices à `eliminates: []` pour qu'ils portent une **mesure** que le joueur doit interpréter contre le rapport, jamais un énoncé de la cause : `s1-h3` peut dire « l'horloge du serveur diverge de la source de référence » sans donner ni le sens ni l'amplitude, laissant `s1-h4` porter le sens. Le chemin frugal légitime (`h1` + `h2` + rapport, 2 indices) est intact et n'a pas besoin d'être touché |
| 🟡 | conform | 1/4 | `config.schema.ts:128-129` · `phase-1.md`, amendement §2 · `phase-4.md:67` | **W1 — Trois documents énoncent une garantie que le code ne donne pas.** Formulation identique aux trois endroits : « le rapport et les indices écartent des alternatives, **jamais ne confirment ou ne nomment la bonne réponse** ». Le schéma n'implémente que la moitié structurelle (`eliminates` ne contient pas la cause `actual`) ; la moitié sémantique n'est vérifiée nulle part, et le corpus livré l'enfreint dans les trois situations (C1). Un contrat qui affirme une propriété qu'il ne tient pas est pire qu'un contrat muet : le prochain auteur de corpus lira ces trois pages et croira le garde-fou posé. C'est exactement le motif que le tour 2 a voulu clore en passant du commentaire au refus — le passage n'a été fait que pour un des deux membres de la phrase | Soit implémenter la seconde moitié (garde-fou de recouvrement sur tous les indices, cf. C1), soit réécrire les trois énoncés pour ne promettre que ce qui est vérifié : « aucun indice ne nomme la cause réelle **parmi ses éliminations** » |
| 🟡 | fit | 4 | `config/course.json:729-798` | **W2 — Deux pratiques opposées ressortent au même endroit, et l'épique demande explicitement le contraire.** Recalculé sur les poids réels (`c1` 2, `c2` 1, `c3` 1, tous sur `pilotage-contexte`) : le profil « demande avant de réfléchir » (un indice confirmant par situation, jamais de cadrage) tient `c1` et manque `c2`/`c3` ⇒ **2/4** ; le profil « cadre juste et d'entrée mais achète tout » manque `c1` et tient `c2`/`c3` ⇒ **2/4**. Les deux sont indiscernables à la sortie, alors que le premier est précisément le comportement que la story existe pour sanctionner. `parcours-couvrant-les-axes.md:32` : « deux joueurs de pratiques opposées […] en ressortent avec des scores d'axes différents ». La cause est entièrement C1 : sans le raccourci à un indice, le premier profil tombe à 0/4 et l'ordre attendu (cadreur frugal 4/4 > cadreur dispendieux 2/4 > demandeur 0/4) se rétablit sans toucher aux poids | Ne rien changer aux poids ni aux seuils : fermer C1 suffit. Vérifier ensuite en test d'intégration qu'un profil « un seul indice, aucun cadrage » ne tient plus `c1` |
| 🟡 | rot | 3/5 | `hint-budget-game.tsx:32` · `phase-5.md:47` · `.impeccable/…:42`, `:68` | **W3 — Quatre endroits affirment que la consigne annonce « que l'ordre des deux gestes est libre ». La consigne ne le dit plus depuis le tour 1.** `phase-4.md:161` documente son retrait, et pour un motif de fond : cette phrase orientait sur la dimension exacte que `c2` note, à contresens de `DESIGN.md`, « Un jeu ne dit jamais ce qu'il note ». Le `statement` livré (`course.json:419`) ne porte plus rien sur l'ordre — vérifié mot pour mot. Mais la docstring du composant décrit encore la consigne comme l'annonçant, et les deux tables « S'énonce / Se tait » de `phase-5.md` et de la fiche de surface la rangent toujours dans la colonne « S'énonce ». Ces deux tables ne sont pas des sections d'historique : elles énoncent le contrat courant. Un auteur qui les suit réintroduira la phrase que le tour 1 a fait retirer | Retirer la ligne des deux tables et la mention de la docstring. Le texte de test `hint-budget-game.test.tsx:62` est une fixture et peut rester |
| 🟡 | conform | 5 | `defects/l-ouverture-…-hors-de-l-ecran.md:28-35` vs `qa/README.md:74,76` | **W4 — Le défaut porte des chiffres que la tournée qu'il cite comme unique preuve contredit.** Son tableau « Actual » : sommet du panneau des causes 1177px desktop / dépassement 277px, 1741px mobile / 897px. `qa/README.md` « Point 3 », refait de zéro au commit `604ece2` : **1164px / 264px** et **1728px / 884px**. Écart de 13px sur les quatre valeurs — la réécriture des `causes[].text` a déplacé le panneau, la tournée a été remesurée, le défaut ne l'a pas été. Son champ `Evidence:55` ne cite pourtant que ce README. Le constat de fond reste vrai dans les deux jeux de chiffres (le panneau est sous le pli aux deux gabarits) ; c'est la traçabilité qui casse, et c'est la troisième fois sur cette branche qu'un document cite une source qui ne dit pas ce qu'il lui fait dire | Reporter les quatre valeurs de la tournée courante |
| 🟡 | code | 4 | `hint-budget-run.test.ts:414-433`, `:442-464` vs `:474-509` | **W5 — Le budget de garde-fous du tour 2 est allé sur la moitié déjà couverte.** Les deux tests ajoutés (« never lets a single hint… », « carries […] a two-hint combination… ») réassertent des refus que `hintBudgetConfigSchema` applique déjà au chargement : `realG2_1Config()` appelle `hintBudgetConfigSchema.parse` en première ligne de chaque test, donc un corpus qui enfreindrait l'un de ces deux invariants lèverait **avant** d'atteindre l'`expect`. Ils ne peuvent pas échouer indépendamment du schéma. Pendant ce temps, le seul garde-fou de corpus qui ne soit pas redondant — le recouvrement lexical, `:474` — reste borné à un indice sur cinq, celui que le même lot a rendu informationnellement vide. Le second test hérite en plus d'un couplage positionnel (`situation.hints[0]`, `[1]` en dur) que le préambule du fichier interdit en toutes lettres (`:39-41`) | Remplacer les deux par l'extension du garde-fou de recouvrement à tous les indices (C1). Si l'intention est de vérifier le corpus réel et pas seulement le schéma, l'assertion utile est celle que le schéma **ne** fait pas |
| 🟢 | code | 3 | `hint-budget-game.tsx:212`, `:228` | `interactive` est toujours passé aux `HintCard` déjà achetées, où `hint-card.tsx:36-40` ne le lit jamais (la branche `bought` rend un cadenas, pas un bouton). Minor du tour 2, non repris | Ne passer `interactive` que sur la branche non achetée |
| 🟢 | code | 4 | `hint-budget-run.test.ts:321` | Le titre dit « solves **at most** one situation », l'assertion dit `toHaveLength(1)` — égalité stricte. Minor du tour 2, non repris ; contredit toujours le préambule `:39-41` du même fichier | `toBeLessThanOrEqual(1)` |
| 🟢 | code | 1 | `config.schema.ts:70`, `:76-79` | `combinationsUpTo` construit `indexes = Array.from({ length }, (_, i) => i)`, un tableau identité : `indexes[i] === i` et `indexes.length === length`. L'abstraction ne porte rien | Boucler sur `length` et empiler `i` |
| 🟢 | conform | 5 | `qa/README.md:105` vs `:23-32` | Le relevé de `s2` est la **seule** mesure citée de la tournée sans capture qui l'ouvre : la table des 16 fichiers ne porte aucune entrée « s2-révélation », et la section « Ce qui n'a pas été mesuré » ne le déclare pas. La tournée a bien joué `s2` à l'aveugle (`:16`) et le chiffre est arithmétiquement juste (`0 + 40 + 30 = 70`), mais c'est une affirmation nue dans un document dont c'est le troisième tour sur ce reproche exact | Une capture `-8-s2-revelation`, ou déclarer la mesure comme non capturée dans la section prévue |
| 🟢 | conform | 5 | `qa/README.md:107` | « `30 < 40` et surtout `30 <= max(cost) = 25` n'est **pas** le cas ici » — la phrase énonce une comparaison fausse (`30 <= 25`) pour dire qu'elle est fausse, et perd le lecteur au passage. La propriété visée est juste | « La surtaxe (30) excède strictement l'indice le plus cher (25) — la garantie sans laquelle le schéma refuse de charger » |
| 🟢 | rot | 1 | `phase-1.md`, Test acceptance criteria, tâche 3 | Le critère « […] rend `framingGrounded: true` et `framedFirst: false`, donc `framedAndGrounded: false` » cite un champ supprimé à la scission du 30/08. `phase-2.md:7` déclare explicitement la convention « cette page garde son contenu d'origine pour l'historique » ; `phase-1.md` n'a pas cette clause, et son amendement ne reprend pas la ligne | Ajouter la même clause d'historique, ou reprendre la ligne |

## Verification

| Metric | Value |
| ------ | ----- |
| Verified | 84% (32/38) |
| Files checked | `config/course.json:410-800` (corpus recalculé indice par indice et cause par cause) · `src/games/hint-budget/**` (11 fichiers) · `__tests__/unit/games/hint-budget/**` (6) · `__tests__/integration/course-run/hint-budget-run.test.ts` · `plan.md` et les 5 phases · `qa/README.md` + 16 PNG (dimensions à l'octet, 3 lus à l'image : `desktop-4`, `desktop-5`, `mobile-6`) · `.impeccable/surfaces/budget-…-hint-budget-game-tsx.md` · les 2 défauts · `stories/acheter-des-indices-a-contrecoeur.md` · `epics/parcours-couvrant-les-axes.md` · `DESIGN.md:60-80` · `config/signature.json` · `src/core/scoring/weighted-mapping.strategy.ts` · historique de `course.json` sur `0d3ae27`, `3308dc0`, `2c6add9`, `6e9ade6`, `604ece2` |
| Unchecked | Phase 1 « les indices ne confirment jamais la bonne réponse » — fix (C1, W1) · Phase 4 règle 4 — fix (C1) · Phase 4 « le chemin frugal qui reste est `h1` + `h2` » — fix (C1) · Phase 3 docstring de la consigne — fix (W3) · Phase 5 mesures du défaut — fix (W4) · Phase 5 « trancher atteignable sans défilement » — reporté au défaut, assumé, non compté |
| Unplanned | Aucun. Les six commits de réparation tracent chacun à un finding nommé des tours 1 ou 2 ; `604ece2` (tournée rejouée) est un correctif de revue hors plan, justifié et documenté dans `phase-5.md` |
