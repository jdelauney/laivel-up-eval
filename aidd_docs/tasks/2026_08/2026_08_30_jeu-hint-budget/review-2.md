# Review 2: Le jeu `hint-budget`, après réparation

- **Verdict**: blocked
- **Diff**: `main...HEAD` (`b7e73ab`..`2c6add9`), réparation `da40c5e`..`2c6add9`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_30
- **Findings**: 3 critical, 9 warning, 7 minor
- **Score**: 62 / 100

Deuxième tour, checker frais. La revue 1 (`review.md`) avait bloqué sur 5 deal-breakers. **Trois sont réellement levés, un est levé en mécanisme mais pas en effet, un est remplacé par un défaut de même nature.** La réparation a par ailleurs ouvert deux trous neufs dans la mesure, tous deux conséquence de l'exécution de la scission de `c2`.

Toutes les simulations ci-dessous sont recalculées par le checker sur le corpus réel de `config/course.json`, en réappliquant les trois règles telles que `hint-budget.evaluator.ts` les écrit. Tests rejoués : `npx vitest run __tests__/unit/games/hint-budget __tests__/integration/course-run/hint-budget-run.test.ts` → 8 fichiers, 81 tests, 0 échec.

## A — L'état des cinq deal-breakers de la revue 1

| # | Deal-breaker de la revue 1 | État | Preuve |
| --- | --- | --- | --- |
| F1 | Corpus traversable sans lire : cause `actual` = texte le plus long, 3/3 | **Levé** (canal résiduel, cf. W7) | Longueurs `causes[].text` : s1 `51,52,52,57,52` · s2 `54,56,59,58,56` · s3 `57,65,51,51,55`. `actual` jamais extrême. Deux garde-fous testés (`hint-budget-run.test.ts:305,319`). Imperceptible à l'écran : `qa/mobile-1-s1-ouverture.png` montre cinq causes sur deux lignes chacune |
| F2 | L'indice le plus cher paraphrase la cause | **Non levé en effet** | La paraphrase littérale est partie et testée (`hint-budget-run.test.ts:339`, LCS < 20). Mais les trois `h5` éliminent désormais **quatre causes sur cinq par leur nom** : acheter `h5` seul résout 3/3 et tient `c1` 3/3. Cf. C1 |
| F3 | `s1` techniquement faux | **Levé** | `s1-h3` (20 min de retard), `s1-h4` (iat jugé futur), `s1-h5`, `s1-c-clock` et sa `verification` portent tous la même direction et la même mesure. 20 min > fenêtre de 15 min et > les 4 min du rapport : le rejet est systématique, pas un cas limite. Vérifié : `iat = T`, horloge serveur `T-16` à l'appel ⇒ émission perçue 16 min dans le futur (précision résiduelle en M2) |
| F4 | Tournée QA qui n'ouvrait pas la région mesurée | **Levé** | Dimensions PNG lues à l'octet : `desktop-1` 1440×**1510** = `scrollHeight` annoncé · `desktop-3` 1440×**1473** (delta −37) · `mobile-1` 390×**2280** · `mobile-3` 390×**2142** (delta −138). Deux captures viewport `-0-` explicitement nommées. Révélation présente et vérifiée au pixel : `desktop-4` porte « INDICES 75 · TRANCHE FAUSSE +40 · TOTAL 115 », exactement `qa/README.md:81` |
| F5 | Renvoi inexact au défaut transverse | **Demi-levé** | Le renvoi est corrigé et bien délimité, un défaut propre est ouvert. Mais son « Expected » attribue à `DESIGN.md` une règle qui n'y figure pas. Cf. C3 |

## Phases

### Phase 1 — Contrats et lecture pure

- [x] Tous les critères de la revue 1 restent tenus ; `framedAndGrounded` supprimé, les deux comptes scindés — `read-situations.helper.ts:144-148`
- [x] `Reading.totalCost` a désormais un consommateur de production — `use-hint-budget.hook.ts:205-211` (F17 clos)

### Phase 2 — L'évaluateur

- [x] Trois règles déclaratives, chacune sur un seul axe — `hint-budget.evaluator.ts:75-95`
- [x] Un cadrage exact posé après un achat manque `c2` et tient `c3` — `evaluator.test.ts`, « misses the order criterion but satisfies the grounding criterion »
- [ ] **La règle `framed-first-at-least` n'a pas de plancher de contenu** : `framing !== null && afterHints === 0` suffit, `retainedIds` peut être vide — `read-situations.helper.ts:69-70`, `answer.schema.ts:15-20`. Cf. C2

### Phase 3 — Le jeu à l'écran

- [x] Affordances mortes fermées aux deux inventaires, verrouillées par test — `hint-card.tsx:44`, `hint-budget-game.tsx:75,84`, `hint-budget-game.test.tsx` « disables every unbought hint button… » (F9 clos)
- [x] Branche morte `selected && !revealed` supprimée — `cause-option.tsx` (F16 clos, mais cf. W5)
- [x] `key={index}` avec `biome-ignore` motivé — `incident-brief.tsx:16-17` (F20 clos)

### Phase 4 — Le corpus et les critères

- [x] Le rapport écarte **exactement** 2 causes sur 5 dans les trois situations — recalculé : s1 `c-secret`+`c-tls` · s2 `c-currency`+`c-discount` · s3 `c-env`+`c-node`. Le troisième fait de chaque rapport oriente sans écarter : règle 1 tenue
- [x] Les suppositions restent non appuyées — les 9 inchangées depuis la revue 1, qui les avait vérifiées une par une
- [x] Rangs déclarés de la cause réelle : 2 · 4 · 1, testé — `hint-budget-run.test.ts:287`
- [x] Le `statement` a perdu la phrase qui rassurait sur l'ordre noté (F12 clos)
- [ ] **`c1` reste gagnable 3/3 par une politique sans compréhension** (C1) et **`c2` 3/3 par une politique sans lecture** (C2)
- [ ] **`phase-4.md:111` est faux**, réfuté par sa propre ligne 103 (W2)

### Phase 5 — La passe de surface

- [x] « Au même coût de geste, **ou l'écart est mesuré, nommé et assumé par écrit** » — la clause d'échappement est d'origine, **non ajoutée par la réparation** (vérifié : `git diff da40c5e^..HEAD -- phase-5.md` n'ajoute qu'une section en fin de fichier, la table d'AC est intacte). Aucun déplacement de poteaux. L'écart est mesuré et assumé
- [x] Grille mobile réparée : `grid-cols-1 sm:grid-cols-2`, aligné sur `cut-panel.tsx:34`. Lisibilité vérifiée sur `qa/mobile-1` : deux à trois lignes par entrée (F10 clos)
- [ ] « Après cinq achats, trancher reste atteignable sans défilement » — **non tenu**, dès l'ouverture, aux deux gabarits (610px / 1436px). Consigné, défaut ouvert, non corrigé
- [x] La tournée est déposée, mesurée à `scrollY = 0`, en page pleine, et son contenu correspond à ce que le README lui attribue

## Findings

### Produit et contrat

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | fit | 4 | `config/course.json:488`, `588`, `688` | **C1 — L'indice le plus cher résout la situation par élimination exhaustive. `c1` (poids 2) est tenu 3/3 par la délégation totale.** Les trois `h5` nomment quatre causes sur cinq pour les écarter : `s1-h5` « élimine le certificat, la clé de signature, l'en-tête et le cache » ⇒ il ne reste que `s1-c-clock`. Idem `s2-h5` (TVA, double calcul, devise, remise ⇒ `c-rounding`) et `s3-h5` (variable, Node, cache, fuseau ⇒ `c-parallel`). **Scénario, simulé sur le corpus réel** : ne rien lire du rapport, acheter `h5` (1 indice sur 5 < 2,5), trancher la cause que `h5` ne nomme pas ⇒ `solved` 3/3, `c1` **TENU 3/3**, déterministe, par simple appariement lexical entre cinq libellés et une liste de quatre noms. Aucune connaissance du domaine, aucun cadrage. C'est le comportement que l'épique nomme mot pour mot comme triche à bloquer (`parcours-couvrant-les-axes.md:32`, « une délégation totale ») et que la story dit mesurer contre (« si je demande avant de réfléchir »). C'est aussi le même effet que F2 de la revue 1 : le mécanisme a changé (paraphrase ⇒ élimination), le résultat non. Le garde-fou ajouté (`hint-budget-run.test.ts:339`, plus longue sous-chaîne commune < 20) mesure exactement le canal qui vient d'être fermé, jamais celui-ci. `phase-4.md:104-109` connaît cette politique, la classe « informée » et l'autorise — c'est là qu'est le désaccord : écarter `n-1` alternatives sur `n`, c'est répondre, pas entourer, et la règle 4 de la même page l'interdit en toutes lettres (« jamais un indice qui répond à la place du joueur ») | Faire en sorte qu'aucun indice seul ne réduise le champ à une cause. Concrètement : `h5` n'écarte que **deux** causes, et le joueur doit croiser `h5` avec le rapport (qui en écarte deux autres) pour arriver à une. Ajouter le garde-fou correspondant : pour chaque situation et chaque indice, le nombre de causes non écartées par cet indice seul reste ≥ 2 |
| 🔴 | fit | 2/4 | `read-situations.helper.ts:69-70`, `answer.schema.ts:15-20`, `hint-budget-game.tsx:81-88` | **C2 — Un cadrage vide posé en premier tient `c2` 3/3. La scission a créé un critère à poids 1 sans plancher.** `framedFirst = framing !== null && afterHints === 0` ; `framingEntrySchema` autorise explicitement `retainedIds: []` (« Un cadrage qui ne retient rien reste un cadrage posé ») ; `postFraming()` ne vérifie rien et le bouton « Transmettre ce cadre » n'est jamais désactivé sur une sélection vide. **Scénario** : ouvrir chaque incident, cliquer « Transmettre ce cadre » sans rien cocher, trancher au hasard ⇒ `framedFirstCount` 3/3, `c2` **TENU 3/3**. Trois clics, zéro lecture, zéro seconde de réflexion. Avant la scission, le critère combiné rendait ce geste inoffensif (vide ≠ fondé) ; la scission l'a transformé en point gratuit sans que la conséquence soit traitée. Combiné à C1 : « transmettre à vide, acheter `h5`, trancher par élimination » ⇒ `c1` + `c2` tenus, soit **3 des 4 points de poids du jeu et 3 des 10 de `pilotage-contexte`** (les 10 = `g2-1` 4 + `g2-2` 3 + `g2-3` 3), sans avoir compris un seul incident. Simulé et vérifié | Donner un plancher à l'ordre : `framedFirst` n'est vrai que si le cadrage retient au moins une lecture (ou : désactiver « Transmettre ce cadre » tant que rien n'est coché, et refuser au schéma un `retainedIds` vide). L'ordre d'un geste vide n'est pas un ordre |
| 🔴 | conform | 5 | `defects/l-ouverture-de-hint-budget-pousse-le-tranchage-hors-de-l-ecran.md:24` | **C3 — Le nouveau défaut cite une règle de `DESIGN.md` qui n'existe pas.** Il écrit : « `DESIGN.md`, section "La surface d'un jeu" : la densité d'un écran ne doit pas retarder le geste central au point de le rendre introuvable sans exploration ». Cette phrase n'est nulle part dans `DESIGN.md`. La section « La surface d'un jeu » (`DESIGN.md:65-77`) porte cinq règles, dont une seule touche au défilement — `:74`, « Un relevé qui s'allonge ne pousse jamais la décision courante hors de l'écran » — et **cette règle-là est tenue** : la tournée mesure que les cinq achats *réduisent* la hauteur du document (−37px desktop, −138px mobile). Le défaut voisin, lui, cite `DESIGN.md:74` **entre guillemets et mot pour mot** (`la-revelation-pousse-l-action-hors-de-l-ecran.md:24`) et son grief y correspond. Le nouveau ticket emprunte la même forme de citation pour une règle inventée : il n'a donc aucune base normative, ce qui le rend inarbitrable au tri. C'est la même faute que F5 de la revue 1 — un renvoi inexact — déplacée du ticket voisin vers `DESIGN.md`. `CLAUDE.md` : « Evidence over assertion » | Soit citer une règle réelle, soit dire franchement qu'aucune règle du produit ne couvre « l'action primaire sous le pli à l'ouverture » et proposer de l'ajouter à `DESIGN.md`. Le constat mesuré est bon ; c'est son fondement qui est fabriqué |
| 🟡 | fit | 5 | `hint-budget-game.tsx:112` | **W1 — L'alternance retenue conserve le biais qu'elle prétend éliminer, parce que le seuil de `c2` est 2 sur 3.** `cadrageFirst = (situationNumber - 1) % 2 === 0` ⇒ cadrage en tête sur `s1` et `s3`, marché en tête sur `s2`. Sur mobile, un joueur qui joue simplement dans l'ordre où la page se présente cadre en premier **2 fois sur 3** — exactement le seuil ⇒ `c2` **TENU** sans intention. L'alternance inverse (marché en tête sur `s1`/`s3`) donnerait 1/3 ⇒ manqué. Avec un nombre impair de situations et un seuil de majorité, aucune alternance n'est neutre, et celle qui a été retenue est la favorable. `phase-5.md:147`, `qa/README.md:44` et `.impeccable/…:34` affirment tous trois que l'alternance « élimine le biais systématique » : c'est vrai de la position des panneaux, faux du verdict, qui est inchangé pour un joueur passif. La revue 1 avait raison de compter cet écart ; la réparation l'a déplacé sans le fermer | Nommer le vrai résultat : l'alternance ne change pas le verdict d'un joueur passif. Deux sorties honnêtes — porter le seuil de `c2` à 3/3 (l'ordre passif ne suffit plus), ou randomiser l'ordre par une graine dérivée de la session plutôt que par la parité du rang. À défaut, retirer des trois documents la phrase « élimine le biais systématique » |
| 🟡 | rot | 4 | `phase-4.md:111` vs `phase-4.md:102-103` | **W2 — Réponse à la question B : le tableau n'est pas faux, c'est sa conclusion qui l'est.** La ligne « Tout cocher » se définit elle-même comme « toutes les lectures, aucun indice, **tranche juste** » : avec cet oracle de tranche injecté, `c1` 3/3 découle mécaniquement (0 indice < 2,5, 3 résolues). La définition est donc complète et la cellule exacte — mais une ligne qui suppose une tranche juste n'est pas une politique aveugle et n'a rien à faire dans un tableau intitulé « Les politiques aveugles » ; la prose de `:108` le concède déjà. Ce qui est **faux**, c'est la phrase de clôture `:111` : « Aucune politique véritablement aveugle […] ne tient plus aucun critère 3/3 ni même 2/3 sur ce corpus. » Sa propre ligne `:103` — « Ne rien cocher (cadrage vide posé en premier, rang fixe) » — porte `c2` **tenu 3/3**, et cette politique-là est intégralement aveugle. Recalculé par le checker : `cadrage vide posé en 1er + rang fixe` ⇒ `c1` 1/3, `c2` **3/3**, `c3` 0/3 ; `cadrage vide + 2e cause la plus longue` ⇒ `c1` **3/3**, `c2` **3/3**, `c3` 0/3. La conclusion fausse est ce qui a laissé C2 passer | Réécrire `:111` sur ce que le tableau montre réellement, et sortir « Tout cocher » du tableau des politiques aveugles ou la renommer « Tout cocher + tranche juste (politique informée) », comme la ligne `h5` l'est déjà |
| 🟡 | fit | 4 | `stories/acheter-des-indices-a-contrecoeur.md:15-20` | **W3 — `g2-1-c3` n'a aucun critère d'acceptation dans la story.** La scission a produit trois critères notés ; la story en porte trois, dont AC2 ↔ `c1` et AC3 ↔ `c2` (« une contextualisation posée avant le premier indice » — désormais exactement ce que `framed-first-at-least` lit, F7 clos de ce côté). Le fondement (`c3`, poids 1) n'est écrit nulle part dans le contrat d'acceptation. La revue 1 (F8) demandait d'amender l'AC3 pour y porter le fondement ; la scission a résolu le décalage dans l'autre sens, en sortant le fondement de `c2` — mais sans jamais l'inscrire au contrat | Ajouter à la story l'AC du fondement, ou consigner dans `plan.md` que `c3` est un garde-fou de mesure hors contrat de story |
| 🟡 | fit | 4 | `config/course.json:491-522` etc. | **W7 — Le canal de longueur suivant : la deuxième plus longue.** Le garde-fou ajouté est une transcription littérale de celui de `lie-detector` (« ni la plus longue ni la plus courte ») et ne couvre que les deux extrêmes. Balayage complet des politiques « k-ième plus longue », tie-break sur l'ordre de déclaration : **k=2 ⇒ 3/3** (`c1` TENU, aveugle) ; tie-break inverse ⇒ 2/3, `c1` TENU aussi. Toutes les autres valeurs de k ⇒ 0 ou 1. Probabilité d'obtenir 3/3 par hasard sur un rang fixe : 1/125 — ce n'est pas du bruit. **Atténuation honnête** : l'écart réel est de 5 caractères (s1 : 57 contre 52·52·52·51), donc humainement imperceptible — `qa/mobile-1` le montre, cinq causes sur deux lignes chacune. Le risque est mécanique, pas jouable à l'œil : d'où 🟡 et non 🔴. Mais le garde-fou ne mesure pas ce qu'il croit mesurer | Remplacer le garde-fou par un invariant ordinal : le rang de longueur de la cause `actual` **diffère d'une situation à l'autre**, comme le fait déjà le garde-fou de rang déclaré (`:287`). Aujourd'hui il vaut 2 · 4 · 4 |
| 🟡 | fit | 3 | `cause-option.tsx:33-58`, `qa/desktop-4-s1-revelation.png` | **W6 — Après une tranche fausse, l'écran ne montre pas quelle cause le joueur a tranchée.** À la révélation, les quatre causes non réelles portent toutes le même cercle fin et le même libellé « ÉCARTÉE » ; rien ne distingue celle qui a été cliquée. Vérifié au pixel sur `desktop-4-s1-revelation.png` : le joueur y a tranché `s1-c-tls`, et cette carte est indiscernable de `c-header`, `c-secret`, `c-cdn`. La revue 1 (F16) proposait deux sorties — supprimer la branche morte, **ou** montrer la sélection — la réparation a pris la première, ce qui ferme le code mort mais laisse le relevé incapable de relier le verdict au geste du joueur. C'est le cœur du critère produit « on comprend pourquoi » de l'épique | Marquer la cause tranchée par une quantité (épaisseur de filet, remplissage), en plus de « ÉCARTÉE ». `DESIGN.md:73` autorise exactement ça |
| 🟡 | conform | 5 | `defects/l-ouverture-…-hors-de-l-ecran.md:47` vs `phase-5.md:141` | **W8 — Deux documents de la même réparation se contredisent sur ce que la mise en page influence.** Le défaut : « Le verdict n'est pas faussé : aucun critère ne lit la position d'un bouton, seulement s'il a été cliqué et quand (`afterHints`). » `phase-5.md:141`, même branche : l'ordre DOM des deux panneaux est « un biais systématique sur la mesure même que `c2` observe ». Or `afterHints` **est** une conséquence de l'ordre dans lequel le joueur rencontre les panneaux, donc de la mise en page — c'est toute la justification de l'alternance. Sur le geste que le défaut couvre (trancher / `c1`), la formulation est juste et bien nuancée (`:20`) ; c'est la généralisation à « aucun critère » qui est fausse | Restreindre la phrase au geste couvert : « la position du panneau des causes ne change pas le verdict de `c1` une fois la tranche posée » |

### Implémentation

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🟡 | rot | 5 | `.impeccable/surfaces/budget-…-hint-budget-game-tsx.md:36` | **W4 — La fiche de surface porte une mesure périmée, contredite par la tournée de la même réparation.** Elle affirme : « acheter les cinq indices d'une situation ne change **pas** la hauteur totale du document (delta mesuré à 0 sur mobile, à −37px sur desktop) ». `qa/README.md:74-76` donne **−138px sur mobile**, et les fichiers le confirment à l'octet : `mobile-1` 390×2280, `mobile-3` 390×2142. Le paragraphe n'a pas été repris alors que le reste de la fiche l'a été | Reporter −138px |
| 🟡 | rot | 5 | même fiche, `:22` et `:45` | **W5 — La fiche décrit encore le jeu d'avant la scission, et remplace une affirmation fausse par une autre.** (a) `:22` : « ce qui est mesuré n'est pas […] mais **deux** choses distinctes » puis décrit le critère combiné (« fondé […] **et** posé en premier ») — le jeu en note trois depuis `3308dc0`. (b) `:45` : la bande « Cadrage / Marché » annonce « deux colonnes » sans le repli `grid-cols-1 sm:`. (c) `:56` corrige à juste titre l'anneau `ring-inset` inexistant (F15) — mais le remplace par « la cause candidate ou tranchée s'annonce par `aria-pressed` », qui ne se produit **jamais** : `CauseOption` ne rend `aria-pressed` que dans sa branche `interactive` (`:86-95`), et `selected` ne peut valoir `true` qu'à la révélation, où `interactive` est faux et où le composant rend un `<div>` sans attribut. Corollaire : `CauseOption.selected` (`cut-panel.tsx:43`) est une **prop morte** — elle n'influence aucune sortie, aucun test ne l'assert (`grep aria-pressed` ne rend que les deux assertions de `FramingLine`) | Reprendre les trois points. Supprimer `selected` de `CauseOption`, ou l'employer pour W6 |
| 🟡 | code | 4 | `hint-budget-run.test.ts:263-276` | **W9 — Un garde-fou perdu, sans remplaçant.** Le test « retenir toutes les lectures » prouvait avant que ce profil ne gagne rien. Il certifie désormais qu'il **gagne `c2`**. Le changement suit la décision produit, il est documenté, et il n'est pas un test réécrit pour passer — mais aucun test ne prend le relais côté « un cadrage sans contenu ne vaut rien ». Le profil qui déclenche C2 (cadrage **vide** posé en premier) n'est couvert nulle part, ni en unitaire ni en intégration | Ajouter le profil « cadrage vide posé en premier » aux profils de `hint-budget-run.test.ts`, avec l'assertion qu'il ne tient aucun critère |
| 🟢 | code | 3 | `hint-budget-game.tsx:201,217` | `interactive` est passé aux `HintCard` déjà achetées, où `hint-card.tsx:36-40` ne le lit jamais (la branche `bought` rend un cadenas, pas un bouton). Même anti-pattern que le `onBuy` que la réparation vient de retirer au même endroit | Ne passer `interactive` que sur la branche non achetée |
| 🟢 | rot | 4 | `config/course.json:488` | `s1-h5` : « aucun jeton n'est accepté, **quel que soit son instant d'émission** » est faux au sens strict. Horloge serveur à `T-16` au moment de l'appel, fenêtre de 15 min ⇒ le serveur accepte tout jeton dont `iat ∈ [T-31, T-16]`, c'est-à-dire émis 16 à 31 minutes avant l'appel. Sans conséquence sur le trafic réel (jetons frais), mais le public visé est composé de développeurs, et c'est précisément la clause absolue qui fait de `h5` un éliminateur exhaustif (C1) | « Aucun jeton fraîchement émis n'est accepté » |
| 🟢 | code | 4 | `hint-budget-run.test.ts:278` | Le titre dit « solves **at most** one situation », l'assertion dit `toHaveLength(1)` — égalité stricte. Une réécriture de corpus qui ferait tomber ce profil à 0 casserait le test alors que la propriété visée tiendrait toujours, ce que le préambule du fichier interdit explicitement (`:40-41`, « une réécriture du corpus ne doit pas casser ce test pour la mauvaise raison ») | `toBeLessThanOrEqual(1)` |
| 🟢 | conform | 4 | `config/course.json:729` | La question de `c1` reste au singulier — « L'incident a-t-il été résolu en achetant moins de la moitié des indices ? » — pour une règle à `threshold: 2` sur trois incidents. Seconde moitié de F7, non traitée ; la première (la question de `c2`) l'est | « Les incidents ont-ils été résolus… », sans annoncer le seuil |
| 🟢 | conform | 5 | `defects/l-ouverture-…-hors-de-l-ecran.md:20` | « son Impact s'appuie **explicitly** sur » — anglicisme dans un document français | `explicitement` |
| 🟢 | conform | 5 | `qa/README.md:36-44` | L'alternance est la réponse centrale de la passe, et la **seule situation qui la démontre** (`s2`, marché en tête) n'a aucune capture : le tableau `:15-23` ne liste que `s1` et `s3`, tous deux cadrage en tête. Les 5 arrêts de tabulation de `s2` sont annoncés sans artefact | Une capture `s2` à l'ouverture, mobile |
| 🟢 | functional | 5 | `phase-5.md:1-3` | Le fichier reste `status: done` alors qu'un critère d'acceptation y est déclaré non tenu (`:149`). L'écart est écrit noir sur blanc et un défaut le suit, donc rien n'est caché — mais la mémoire projet demande que les phases suivent le code | `status: done-with-gaps`, ou la convention équivalente du projet |

### Vérifié bon, à dire aussi

| Point | Preuve |
| --- | --- |
| **Pas de déplacement de poteaux** | La clause d'échappement « ou l'écart est mesuré, nommé et assumé par écrit » de `phase-5.md:127` est **d'origine**. `git diff da40c5e^..HEAD -- phase-5.md` n'ajoute qu'une section terminale : la table d'acceptation n'a pas été touchée. La réparation n'a réécrit aucun de ses juges |
| **Aucun test réécrit pour passer** (question D) | Aucun test supprimé. Les trois renommages d'`evaluator.test.ts` **conservent** l'assertion d'origine et en **ajoutent** une (`orderCriterion` false **et** `groundingCriterion` true, là où il n'y avait qu'un booléen). Un test neuf couvre le cas que la scission existe pour traiter (cadrage premier mais incomplet ⇒ `c2` vrai, `c3` faux). `read-situations.test.ts` perd trois assertions sur `framedAndGrounded` parce que le champ n'existe plus, et en gagne une sur `framedFirstCount`. Quatre garde-fous de corpus ajoutés. Seule perte réelle : W9 |
| **La tournée QA est désormais probante** | Chaque mesure du README est corroborée par la dimension du PNG correspondant, lue indépendamment : 1440×1510 / 1440×1473 / 390×2280 / 390×2142. Les deux captures viewport sont nommées comme telles. La révélation manquante de la revue 1 est là et son relevé est exact (5+10+15+20+25 = 75, +40 de tranche fausse, total 115, pas de surtaxe d'aveugle puisque des indices ont été achetés) |
| **`s1` est techniquement juste** | Recalculé : horloge serveur en retard de 20 min, jeton émis 4 min avant l'appel ⇒ le serveur perçoit l'émission 16 min dans son futur ⇒ rejet pour `iat` future, ce que dit `s1-h4`. La fenêtre de 15 min n'est jamais en cause, l'expiration non plus. `h3`, `h4`, `h5`, `c-clock` et sa `verification` disent tous la même chose. La contradiction interne de la revue 1 est intégralement levée |
| **Le corpus tient les règles 1, 3 et 5** | Le rapport écarte exactement 2 causes sur 5 dans les trois situations, recalculé cause par cause. Rangs déclarés de la cause réelle 2 · 4 · 1, testé. Ensembles de rangs des lectures établies distincts, testé |
| **La lisibilité mobile est réellement corrigée** | `qa/mobile-1-s1-ouverture.png` (390×2280, page pleine) : chaque lecture de cadrage tient sur deux lignes, chaque indice sur une à deux. Plus rien qui se replie caractère par caractère. Et l'incohérence des deux grilles du même écran est fermée dans le même geste |
| **Collision de libellé fermée** | « SITUATION 1 SUR 20 » (coquille) et « INCIDENT 1 SUR 3 » (jeu) cohabitent sans dénominateurs contradictoires — visible sur toutes les captures de la tournée |
| **Affordances mortes fermées, et testées** | `hint-card.tsx:44` et `hint-budget-game.tsx:75,84` portent `disabled` ; le test « disables every unbought hint button and every framing reading once the situation is revealed » couvre les cinq indices **et** les cinq lectures |
| **Aucune fuite dans le DOM** | Inchangé et toujours tenu : `FramingLine` ne reçoit que `text`, le hook n'expose ni `established`, ni `actual`, ni `verification`, ni le `text` d'un indice non acheté. Les deux tests de comparaison d'arbres rendus sont intacts |
| **`c3` fait son travail** | Toutes les politiques simulées, sans exception, manquent `c3` 0/3. Le fondement exact est le seul des trois critères qu'aucune politique mécanique n'atteint. C'est le critère qui sauve la mesure aujourd'hui — et il ne pèse que 1 sur 10 |

## Verification

| Metric | Value |
| ------ | ----- |
| Verified | 89% (34/38) |
| Files checked | `config/course.json:410-770`, `src/games/hint-budget/**` (11 fichiers), `__tests__/unit/games/hint-budget/**` (6), `__tests__/integration/course-run/hint-budget-run.test.ts`, `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/**` (plan, 5 phases, `qa/README.md`, 14 PNG mesurés), `.impeccable/surfaces/budget-…-hint-budget-game-tsx.md`, les deux défauts de `aidd_docs/backlog/defects/`, `stories/acheter-des-indices-a-contrecoeur.md`, `epics/parcours-couvrant-les-axes.md`, `DESIGN.md:60-90` |
| Unchecked | Phase 2 « la règle d'ordre a un plancher de contenu » — fix (C2) · Phase 4 « aucune politique aveugle ne tient un critère » — fix (C1, C2) · Phase 4 « `phase-4.md` dit vrai de son propre corpus » — fix (W2) · Phase 5 « trancher atteignable sans défilement » — reporté au défaut, non corrigé (assumé) |
| Unplanned | Le renommage « Situation » ⇒ « Incident » et le `biome-ignore` de `incident-brief.tsx` sont des correctifs de revue hors plan, tous deux justifiés et documentés |

## Score

**62 / 100.** Aucun seuil de passage n'est défini par le mandat ; ce chiffre est un rapport, pas un verdict de recevabilité.

Le calcul : la réparation lève réellement trois des cinq deal-breakers, avec des preuves que j'ai pu reproduire indépendamment (dimensions PNG, cohérence technique de `s1`, décompte des causes écartées) — c'est du travail sérieux, et la tournée QA reprise est exemplaire. Mais le jeu note quatre points de poids, et **trois d'entre eux sont aujourd'hui atteignables sans comprendre un seul incident** : `c1` (poids 2) par l'achat de `h5` et une élimination lexicale, `c2` (poids 1) par un cadrage vide transmis en premier. Seul `c3` (poids 1) résiste. Un jeu dont la story dit « on mesure si je cadre avant de demander, ou si je demande avant de réfléchir » et qui récompense au maximum celui qui demande sans réfléchir ne mesure pas ce qu'il annonce. C'est ce qui plafonne la note, indépendamment de la qualité de l'implémentation, qui est bonne.
