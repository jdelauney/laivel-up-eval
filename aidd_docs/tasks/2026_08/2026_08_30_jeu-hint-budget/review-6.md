# Review: Le jeu `hint-budget` — 6e tour

- **Verdict**: changes-requested
- **Diff**: `main...HEAD` (`b7e73ab`..`130225a`, 24 commits)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_31
- **Findings**: 1 critical, 5 warning, 7 minor

## Phases

### Phase 1 — Les contrats et la lecture pure des situations

- [x] Une situation dont toutes les lectures de cadrage sont établies est refusée au chargement, en nommant la situation — `config.schema.ts:285-291`
- [x] Une situation à zéro ou deux causes `actual` est refusée, en nommant la situation — `config.schema.ts:253-271`
- [x] Une configuration dont la surtaxe d'aveugle n'excède pas l'indice le plus cher est refusée, en nommant les deux montants — `config.schema.ts:525-531` (`30 > 25` sur le corpus livré)
- [x] Une trace qui omet une situation, tranche une cause inconnue, ou dont `afterHints` dépasse les achats, lève l'erreur nommée portant l'identifiant fautif — `answer.schema.ts:193,206,227`
- [x] Une trace qui achète deux fois le même indice dans une situation est refusée par le schéma — `answer.schema.ts:56-59`
- [x] Un cadrage retenant toutes les lectures établies et aucune supposition rend `framingGrounded: true`, `false` dès qu'on en retire ou ajoute une — `read-situations.helper.ts:83-91`
- [x] Un cadrage exact posé après un achat rend `framingGrounded: true` et `framedFirst: false` — donc `c2` manqué, `c3` tenu. **Fermé au tour 5** : `phase-1.md:133` ne cite plus `framedAndGrounded`, et `phase-1.md:7` porte la clause d'historique qui couvre les mentions restantes
- [x] Une tranche fausse sans indice coûte strictement plus qu'une tranche fausse après l'indice le plus cher — `config.schema.ts:525-531` + `hint-budget-run.test.ts:641`
- [x] `typecheck` / `test` passent — 667 tests, 73 fichiers, exit 0 ; `npx biome check src __tests__ config` : « Checked 201 files in 62ms. No fixes applied. ». `npm run lint` échoue au niveau dépôt via un worktree tiers de `.claude/worktrees/` portant son propre `biome.json` : condition préexistante, étrangère à cette branche

### Phase 2 — L'évaluateur et ses trois règles

- [x] Une trace qui achète exactement la moitié des indices ne fait pas compter la situation comme frugale — `hint-budget.evaluator.ts:56-59` (inégalité stricte)
- [x] Une trace sans achat et sans bonne cause ne satisfait pas la frugalité (`solved &&`) — `hint-budget.evaluator.ts:56-59`
- [x] Un cadrage exact posé après un achat ne compte pas pour le critère d'ordre — `read-situations.helper.ts:78-81`
- [x] Un `rule.type` inconnu lève une erreur nommée citant le type et le jeu — `hint-budget.evaluator.ts:35-40,131`
- [x] `typecheck` / `test` passent

### Phase 3 — Le jeu à l'écran : cadrer, acheter, trancher

- [x] Deux parties aux mêmes gestes dans des ordres différents produisent une trace identique — `build-hint-budget-answer.action.ts:30-34` (l'ordre suit la configuration, jamais le jeu)
- [x] Une fois le cadre déposé, aucune lecture ne change d'état — `use-hint-budget.hook.ts:99-106`, `framing-line.tsx:25`
- [x] Acheter puis déposer le cadre est accepté, `afterHints` à 1 — `use-hint-budget.hook.ts:109-115`
- [x] Aucune manière d'acheter plus d'un indice par appel — `use-hint-budget.hook.ts:121-124`, `hint-card.tsx:36-50`
- [x] `advance()` deux fois à la dernière situation ne soumet qu'une trace — `use-hint-budget.hook.ts:161-165`
- [x] Le rendu avant révélation ne porte ni `verification`, ni `actual`, ni `established`, ni `refersTo`, ni le texte d'un indice non acheté — `use-hint-budget.hook.ts:168-196`, vérifié en capture `qa/desktop-1-s1-ouverture.png`
- [x] `typecheck` / `test` passent

### Phase 4 — Le jeu dans le parcours, et son corpus

- [x] Ajouter le jeu n'a demandé que les deux blocs de câblage — `register-games.ts`, `register-components.ts`
- [x] Le parcours réel se charge sans refus, et `g2-1` passe `hintBudgetConfigSchema` — `hint-budget-run.test.ts:198`
- [x] `checkpoints-run` et `three-tracks-run` traversent les sept groupes sans refus — `__tests__/fixtures/hint-budget-answer.ts`
- [x] Un profil qui cadre juste d'entrée et résout **les trois** situations avec au plus deux indices satisfait **les trois** critères — **fermé au tour 5** : le critère est réécrit (`phase-4.md:191`) et `frugalFramerAttempts` joue désormais les trois situations correctement (`hint-budget-run.test.ts:120-127`)
- [x] Un profil qui ne cadre jamais et achète tous les indices manque les trois — `hint-budget-run.test.ts:234`
- [x] Trancher systématiquement la première cause résout au plus une situation — `hint-budget-run.test.ts:311`
- [x] Retenir toutes les lectures satisfait `c2` et manque `c3` — `hint-budget-run.test.ts:274`
- [x] `typecheck` / `test` passent

### Phase 5 — La passe impeccable de la surface

- [x] La fiche de surface existe et nomme la stratégie retenue — `.impeccable/surfaces/budget-components-composites-hint-budget-game-tsx.md`
- [x] Mobile : l'écart de coût de geste est mesuré, nommé et assumé par écrit — `qa/README.md` Points 1, 2 et 4 (marché en tête sur `s1`/`s3`, cadrage sur `s2` ; 5 puis 6 arrêts de tabulation)
- [x] Une lecture établie et une supposition sont rendues avec exactement la même structure — `framing-line.tsx` ne reçoit ni `established` ni `refersTo`, test d'égalité stricte de balisage `hint-budget-game.test.tsx:282-319`, vérifié en capture `qa/desktop-1-s1-ouverture.png`
- [ ] Après cinq achats, l'action de trancher reste atteignable sans défilement aux deux gabarits — non tenu, dès l'ouverture : 353,5px sous le pli desktop, 957px mobile (`qa/README.md` Point 3). Différé sur `aidd_docs/backlog/defects/l-ouverture-de-hint-budget-pousse-le-tranchage-hors-de-l-ecran.md`
- [x] Un test échoue si la présentation d'une lecture dépend de sa nature — `hint-budget-game.test.tsx:282`
- [x] Un test échoue si la révélation qualifie le cadrage — `hint-budget-game.test.tsx:341`
- [x] `typecheck` / `test` passent
- [x] La tournée aux deux gabarits est déposée dans `qa/`, mesurée à `scrollY = 0` vérifié — 18 captures, dimensions PNG recoupées une à une avec la table du README : **aucun écart sur les 18**

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | code | 1 | `src/games/hint-budget/schema/config.schema.ts:486-492` (le plancher), `:440-453` et `:455-468` (les deux refus `refersTo`) · `__tests__/unit/games/hint-budget/config.schema.test.ts` (aucune occurrence) | **Les trois refus produits par les tours 4 et 5 — le livrable central de la branche — ne sont vérifiés par aucun test.** `config.schema.test.ts` couvre séparément les onze autres refus (`:94` à `:398`) mais pas ceux-là. `refersTo` n'apparaît dans les six fichiers de test que comme paramètre d'assistant à valeur par défaut `null` (`config.schema.test.ts:7`, `answer.schema.test.ts:19`, `evaluator.test.ts:12`, `read-situations.test.ts:11`, `build-answer.test.ts:12`, `use-hint-budget.test.ts:8`) : aucun test ne lui donne jamais une valeur non nulle, donc aucun n'exerce le mécanisme entier du tour 5. Un refus n'est observable qu'à travers une configuration qui le viole ; aucune n'existe, et aucun test n'assertionne les messages « ne laisse que N cause(s) jamais nommée(s) », « désigne … absente de ses causes », « désigne la cause réelle ». Conséquence : **supprimer le bloc `standingCauses.length < 2` laisse les 667 tests verts** — le test d'intégration `hint-budget-run.test.ts:472` recalcule l'union lui-même sur le corpus réel, sans jamais passer par le schéma, et les quatre tests qui partagent le `path` `['situations', i, 'causes']` (`:154`, `:169`, `:250`, `:283`) déclenchent leur propre refus avant celui-ci dans l'ordre du `superRefine`. C'est exactement le trou que le tour 5 a relevé sur le seuil de `c1` (« ramener `threshold` à `2` laisse les 665 tests verts »), refermé pour `c1`, rouvert au même endroit pour le garde-fou qui compte le plus. La doctrine de la branche est « on passe le garde-fou en contrat, pas en consigne » (`plan.md:46`) : un contrat sans test est une consigne | Trois cas dans `describe('the cause-elimination graph', …)` : une situation dont l'union rapport ∪ indices ∪ `refersTo` ne laisse qu'une cause debout (attendre le message du plancher, `path` `['situations', 0, 'causes']`) ; une lecture dont `refersTo` cite une cause absente ; une lecture dont `refersTo` désigne la cause `actual` |
| 🟡 | fit | 4 | `config/course.json:535`, `:629`, `:758` | **Relevé au tour 5, non corrigé : la révélation écarte trois causes par un fait que le joueur ne peut pas obtenir.** `s1-c-cdn` (« La réponse est marquée non-cacheable et n'entre jamais dans le cache CDN »), `s2-c-double` (« Le total n'est calculé qu'une seule fois, côté serveur »), `s3-c-cache` (« Le cache de dépendances de l'agent CI est reconstruit à l'identique, sans corruption ») : aucun de ces trois faits n'est dans un `symptom`, une ligne de `report`, un `label` ou un `text` d'indice — vérifié par recherche plein texte sur la configuration ; ils viennent des trois indices supprimés par `69633bd`. `git diff 69633bd..HEAD -- config/course.json | grep verification` ne rend **aucune ligne** : les trois `verification` n'ont pas bougé. Les douze autres causes citent le rapport ou un indice achetable mot pour mot (`s1-c-header` ↔ `s1-h1`, `s2-c-vat` ↔ `s2-h5`, `s3-c-timezone` ↔ `s3-h4`, …). Or ces trois-là sont précisément les survivantes que `phase-4.md:219-223` désigne comme départagées **par inférence sur le rapport** : le corpus contredit sa propre table de conception, sur les trois cartes qui portent tout le raisonnement. Scénario : le joueur infère « un échec jamais sur les mêmes tests exclut un cache corrompu », tranche `s3-c-parallel`, et la révélation lui répond par un état du cache qu'il n'a jamais pu observer. Visible sur `qa/desktop-4-s1-revelation.png` (carte « Le cache CDN sert une version périmée de la réponse ») | Réécrire les trois `verification` sur la ligne de rapport que `phase-4.md:221-223` désigne déjà mot pour mot : « un cache resservirait la même réponse », « un double calcul frapperait toutes les lignes », « un cache corrompu échouerait de façon déterministe » |
| 🟡 | rot | 1 | `config/course.json:532`, `:626`, `:755` (`ruledOutByReport: false`) · doc du champ `src/games/hint-budget/schema/config.schema.ts:73-75` | **`ruledOutByReport` est déclaré `false` sur les trois causes que le rapport écarte réellement — et c'est la seule raison pour laquelle deux garde-fous passent.** Le champ est documenté « Le rapport gratuit écarte-t-il déjà cette cause ». Or `s1` ligne 4 (« jamais deux fois la même réponse servie à l'identique ») écarte `s1-c-cdn`, `s2` ligne 3 (« que sur les lignes dont la quantité comporte une décimale ») écarte `s2-c-double`, `s3` ligne 3 (« jamais sur les mêmes tests d'une exécution à l'autre ») écarte `s3-c-cache` — c'est ce que `phase-4.md:219-223` affirme explicitement. Déclarés `true`, `remainingAfterReport` tomberait à `2` et le refus `:405` rejetterait le corpus livré ; `standingCauses` tomberait à `1` et le plancher `:486` le rejetterait aussi. Les deux refus mesurent donc ce que l'écran **nomme**, jamais ce qu'il **écarte**, alors que le champ et son message (« le rapport … écarte N cause(s) ») disent l'inverse. Ce n'est pas une fuite — l'inférence reste requise — mais c'est la couture sémantique par laquelle les fuites des tours 4 et 5 sont revenues, et elle est toujours ouverte pour le prochain corpus | Renommer le champ sur ce qu'il mesure (`namedByReport`, ou `ruledOutByReportVerbatim`) et reformuler les deux messages de refus, ou assumer par écrit dans le docblock que « écarter » y veut dire « nommer », l'élimination inférentielle étant délibérément hors contrat |
| 🟡 | rot | 3 | `src/games/hint-budget/components/composites/hint-budget-game.tsx:31-34` · `.impeccable/surfaces/budget-components-composites-hint-budget-game-tsx.md:42,68` | **Trois emplacements affirment que la consigne annonce « que l'ordre des deux gestes est libre » — la phrase a été retirée délibérément.** `phase-4.md:199` : « Le `statement` a perdu sa phrase … elle rassurait sur la dimension exacte que `c2` note, à contresens de `DESIGN.md:77` ». Le `statement` livré (`config/course.json`) ne dit rien de l'ordre. La fiche de surface le classe pourtant en colonne « S'énonce » (`:68`), face à « Qu'il en existe un qui vaut mieux » en colonne « Se tait » — c'est-à-dire qu'elle documente comme énoncé ce qu'une décision produit a fait taire | Retirer le troisième membre du docblock `:33` et la ligne `:68` de la table ; corriger « ordre libre » en `:42` |
| 🟡 | code | 1 | `__tests__/unit/games/hint-budget/config.schema.test.ts:31-40` et `:382` | Le docblock du corpus de test et le nom du test décrivent le contrat d'avant le tour 4. Le docblock : « le chemin frugal exige de faire tomber le champ à **une** cause » et « aucun indice seul ne ramène le champ sous deux » — le premier refus exige aujourd'hui de le ramener **à deux** (`config.schema.ts:511-513`), le second a été supprimé par `69633bd`. Le test `:382` s'intitule « … narrows the field to **one** cause » pour un refus dont le message dit « aux deux dernières causes ». Son assertion (`path` `['situations', 0, 'hints']` + message contenant `'s1'`) ne distingue en outre pas ce refus de son voisin `:392` (« aucun indice ne vise une cause déjà écartée »), qui porte le même `path` et le même identifiant | Réécrire le docblock sur le plancher de deux, renommer le test, et assertionner un fragment de message propre (« aux deux dernières causes ») |
| 🟡 | rot | 4 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/phase-4.md:109` | Renvoi pendant : « Le détail des probabilités de chance … vit dans « Trois chances, mesurées et non arrondies » plus bas ». Aucune section de ce nom n'existe — le titre a été réécrit au tour 5 en « Quatre politiques mesurées, non arrondies (tour 3, recalculées au tour 4) » (`:135`), sans mettre à jour le pointeur qui y mène | Citer le titre courant |
| 🟢 | rot | 4 | `phase-4.md:233` | « Les **cinq** lectures `established` gardent leur texte » — le corpus en porte **six**, deux par situation (`s1-f1`/`s1-f2`, `s2-f2`/`s2-f4`, `s3-f3`/`s3-f5`) | Corriger le compte |
| 🟢 | rot | 5 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/qa/README.md:122` | Point 6 : « les cinq lectures du panneau « Le cadrage » de `s1` (deux établies désormais dotées d'un `refersTo`, **trois** suppositions réécrites) ». Deux seulement l'ont été (`s1-f3`, `s1-f4`) ; `s1-f5` garde son texte d'origine — ce que la ligne 3 du même fichier dit correctement en listant les six réécritures. Le fichier se contredit à 119 lignes d'écart | Écrire « deux suppositions réécrites, une inchangée » |
| 🟢 | rot | 5 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/phase-5.md:199` · `.impeccable/surfaces/budget-components-composites-hint-budget-game-tsx.md:84` | « tour 6 » pour la tournée jouée après `ec18346`. Les sections précédentes numérotent la tournée d'après la revue qui l'a déclenchée (« tour 3 — après la réécriture des indices », « tour 4 — après `69633bd``) ; `ec18346` répond à `review-5.md`, donc « tour 5 ». C'est aussi la cinquième reprise, pas la sixième, dans les deux comptages possibles | Renuméroter en « tour 5 (31/08) » aux deux endroits |
| 🟢 | rot | 1 | `phase-1.md:17` | Le commentaire de l'arbre annonce « ses **dix-huit** refus (après le tour 5) » ; `config.schema.ts` porte **dix-neuf** `context.addIssue` (`grep -c` = 19). Le compte manque le second refus `refersTo` : `phase-1.md:184` annonce « deux refus … s'ajoutent aux dix-huit », alors que la base était de dix-sept | Corriger en dix-neuf |
| 🟢 | rot | 4 | `phase-4.md:202` | L'amendement décrit toujours le garde-fou de recouvrement comme portant sur « **l'indice le plus cher** » ; il a été généralisé à tous les indices au tour 3 (`hint-budget-run.test.ts:591`, et `phase-1.md:166` documente correctement la généralisation). Deux pages du même dossier décrivent deux portées différentes du même test | Aligner sur `phase-1.md:166`, ou marquer l'amendement comme historique |
| 🟢 | rot | 4 | `phase-4.md:178-181` | « **Trois** garde-fous de corpus, vérifiés sur le corpus réel », suivi de trois puces ; `hint-budget-run.test.ts` en porte huit (`:320`, `:338`, `:352`, `:377`, `:410`, `:438`, `:472`, `:591`, `:628`). Section non marquée comme historique, contrairement à `phase-1.md:7` et `phase-2.md:7` | Poser la même clause d'historique en tête de la section « Tasks to do », ou mettre le compte à jour |
| 🟢 | fit | 4 | `config/course.json` (`g2-1`, les trois `causes[].text` réelles) | **Observation produit, pas une fuite mécanique : dans les trois situations, la cause réelle est la réponse canonique du symptôme et la survivante-leurre est invraisemblable de face.** « 401 depuis le déploiement, passe en local » → dérive d'horloge ; « total faux d'un centime sur certaines lignes » → arrondi par ligne ; « tests verts en local, rouges en CI » → parallélisme sans isolation. Face à elles : un cache CDN qui servirait un 401 authentifié, un total « recalculé deux fois de façon indépendante » (qui, recalculé à l'identique, ne produit aucun écart), un cache de dépendances corrompu. Un développeur expérimenté tient `c1` 3/3 sans lire le rapport ni acheter. Ce n'est pas la triche que l'épique interdit — raisonner *est* ce que la story mesure, et `c2`/`c3` restent intacts — mais `c1` (poids 2 sur 4) note surtout la connaissance du domaine, pas la discipline de frugalité que son intitulé annonce. Trois situations ne permettent aucune correction statistique | Rien d'actionnable dans cette branche. À consigner dans `phase-4.md` à côté de « La qualité de ces trois inférences reste éditoriale » (`:225`), qui dit déjà la moitié de ce constat |
| 🟡 | functional | 5 | `phase-5.md:129` · `qa/README.md` Point 3 | Critère non tenu : trancher est sous le pli dès l'ouverture, aux deux gabarits (353,5px desktop, 957px mobile). Décision produit de différer, défaut suivi | Hors périmètre de cette branche selon `phase-5.md:207` ; laissé à `l-ouverture-de-hint-budget-pousse-le-tranchage-hors-de-l-ecran.md` |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 97% (36/37)                                       |
| Files checked | `config/course.json`, `src/games/hint-budget/schema/config.schema.ts`, `src/games/hint-budget/schema/answer.schema.ts`, `src/games/hint-budget/helpers/read-situations.helper.ts`, `src/games/hint-budget/hint-budget.evaluator.ts`, `src/games/hint-budget/hooks/use-hint-budget.hook.ts`, `src/games/hint-budget/actions/build-hint-budget-answer.action.ts`, `src/games/hint-budget/components/composites/hint-budget-game.tsx`, `src/games/hint-budget/components/composites/cut-panel.tsx`, `src/games/hint-budget/components/composites/incident-brief.tsx`, `src/games/hint-budget/components/elements/hint-card.tsx`, `src/games/hint-budget/components/elements/framing-line.tsx`, `src/games/hint-budget/components/elements/cause-option.tsx`, `src/games/register-games.ts`, `src/games/register-components.ts`, `__tests__/integration/course-run/hint-budget-run.test.ts`, `__tests__/unit/games/hint-budget/*`, `__tests__/fixtures/hint-budget-answer.ts`, `aidd_docs/tasks/2026_08/2026_08_30_jeu-hint-budget/plan.md`, `phase-1.md`, `phase-4.md`, `phase-5.md`, `qa/README.md`, les 18 captures de `qa/` (dimensions PNG lues à l'octet), `.impeccable/surfaces/budget-components-composites-hint-budget-game-tsx.md`, `aidd_docs/backlog/stories/acheter-des-indices-a-contrecoeur.md` |
| Unchecked     | Phase 5 « Après cinq achats, trancher reste atteignable sans défilement » — fix (différé, défaut suivi) |
| Unplanned     | none |
