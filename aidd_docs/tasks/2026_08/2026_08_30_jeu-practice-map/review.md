# Review: Le jeu `practice-map`, situer les pratiques sur deux axes

- **Verdict**: changes-requested
- **Diff**: `main...HEAD` (`be70886`, `bcf1762`)
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_31
- **Findings**: 1 critical, 6 warning, 5 minor

## Phases

### Phase 1 — Les contrats et la lecture pure des placements

- [x] Un corpus dont deux zones se touchent, dont une zone dépasse 12 %, dont l'emprise dépasse la moitié, dont les zones sont d'un seul côté d'un axe, ou dont une relation n'est pas soutenue par les zones, est refusé en nommant le champ — `src/games/practice-map/schema/config.schema.ts:245-331`, couvert par `__tests__/unit/games/practice-map/config.schema.test.ts:171-285`
- [x] Trace incomplète → `IncompletePlacementError` nommant la pratique ; pratique inconnue → `UnknownPracticeError` — `src/games/practice-map/schema/answer.schema.ts:47-96`
- [ ] Bordure lue dedans ; égalité ne tient pas la relation ; **le helper ne lit aucun seuil** — les deux premières clauses tiennent (`helpers/read-placements.helper.ts:46-54`, `:118`), la troisième non : `read-placements.helper.ts:83` lit `config.highRigorFrom`, et le docblock `:9-13` affirme l'inverse mot pour mot
- [x] La trace suit l'ordre des pratiques de la configuration, sans champ dérivé — `actions/build-practice-map-answer.action.ts:36-42`, `schema/answer.schema.ts:13-17`
- [x] `npm run test` passe, chaque refus a son test isolant — 649 tests / 73 fichiers verts, 18 cas dans `config.schema.test.ts`

### Phase 2 — L'évaluateur et ses trois règles

- [x] Trois règles à verdict binaire, une lecture chacune, type inconnu → erreur nommant le type — `src/games/practice-map/practice-map.evaluator.ts:105-122`, `:31-36`
- [x] Lecture décalée en bloc → `c1` manqué, `c3` satisfait ; sept jetons empilés → trois critères manqués — `__tests__/unit/games/practice-map/evaluator.test.ts:136`, `:151`, `__tests__/integration/course-run/practice-map-run.test.ts:208`, `:230`

### Phase 3 — Le jeu à l'écran : poser, déplacer, soumettre

- [x] Reposer remplace le placement ; soumission sans effet réserve non vide ; une seule soumission ; aucun repère lisible avant la révélation — `hooks/use-practice-map.hook.ts:111-118`, `:141-145`, `:148-167`, `:192-199`, test `practice-map-game.test.tsx:204`
- [ ] Aucune ligne de quadrant *(contrainte annulée par le chef, `phase-5.md:115` — non retenue contre le diff)* ; saisie/déplacement/dépôt au clavier seul **[x]** `components/composites/practice-plane.tsx:166-196` ; position annoncée en mots dans un `aria-live` **[ ]** — l'annonce bascule sur `highRigorFrom` (`use-practice-map.hook.ts:210-212`) et divulgue un seuil de notation ; aucun état porté par la couleur seule **[x]** `elements/practice-token.tsx:40-47`
- [x] `npm run test` passe, le parcours clavier est couvert — `practice-map-game.test.tsx:255-296` ; la frontière de l'annonce et le retour de focus après dépôt ne le sont pas

### Phase 4 — Le jeu dans le parcours, et son corpus

- [x] `practice-map` résout évaluateur et composant, aucun autre câblage n'a bougé — `src/games/register-games.ts:68-72`, `src/games/register-components.ts:26`
- [x] Le `course.json` réel charge ; sept zones disjointes, chacune sous 12 %, sept relations soutenues — recalculé indépendamment : aires 0.050 / 0.075 / 0.075 / 0.0625 / 0.036 / 0.050 / 0.0575, total 0.406, 21 paires disjointes, 7 relations soutenues
- [x] Sept jetons empilés, comme sept jetons sur une diagonale unique, manquent les trois critères — `practice-map-run.test.ts:208`, `:219`, reconfirmé par simulation
- [x] Lecture juste → trois critères ; lecture nulle → aucun ; lecture décalée → `c1` manqué, `c3` tenu ; `npm run test` et `npm run typecheck` passent — `practice-map-run.test.ts:183`, `:208`, `:230`

### Phase 5 — La passe impeccable de la surface

- [ ] Plan carré à toutes les tailles **[x]** ; sans ligne de quadrant *(annulé par le chef)* ; **la réserve pleine ne pousse ni le plan ni l'action primaire hors de l'écran [ ]** — 713-775px de dépassement mobile, 193-245px desktop, défaut de backlog ouvert et arbitré ; aucune animation d'étape **[x]**
- [x] La fiche de surface existe et consigne le refus des lignes de quadrant et la mise en page mobile — `.impeccable/surfaces/ce-map-components-composites-practice-map-game-tsx.md`
- [x] Les captures des trois temps existent aux deux gabarits ; `npm run test` et `npm run typecheck` passent — 12 PNG sous `qa/`

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | conform | 3 | `src/games/practice-map/hooks/use-practice-map.hook.ts:210-212` | `positionLabel` choisit le mot de rigueur sur `rigor < parsed.highRigorFrom`. Le seuil de notation est donc annoncé à l'écran : depuis le centre, deux `ArrowUp` (0.5 → 0.6 → 0.7) font basculer l'annonce de « rien ne la vérifie » à « un garde-fou la tient sans vous », localisant `highRigorFrom` dans `]0.6, 0.7]`. Viole `DESIGN.md:77` (« Un jeu ne dit jamais ce qu'il note »), contredit `phase-5.md:119` (« jamais au seuil réel `highRigorFrom` ») et la fiche de surface (« Se tait : Le seuil de haute rigueur »). Exploit mesuré : poser les sept jetons sur une ligne horizontale à rigueur ≥ 0.75, intensités étalées, ordre des pratiques au hasard, sans lire un libellé, tient `c2` à **47,6 %** contre 13,3 % au hasard — au-dessus des 43,75 % que `plan.md:32` cite comme le coût inacceptable de cellules de dépôt. La même ligne posée à 0.60, le haut de la croix visible, tient `c2` à **0 %** : c'est l'annonce qui transforme une intuition fausse en stratégie gagnante | Couper le cran de rigueur au même 0.5 que la croix dessinée (`practice-plane.tsx:220`), ou retirer le cran de rigueur de l'annonce et n'y garder que l'intensité, dont le repère 0.5 est déjà visible |
| 🟡 | fit | 4 | `config/course.json` critère `g2-2-c2` | La question affichée dit « Une pratique de haute rigueur a-t-elle été située dans **son quadrant** ? », la règle exige la **zone** de la pratique. Le quadrant haut-droit dessiné couvre ~15 % du plan, la zone de `p3` 7,5 %. Un joueur qui pose `p3` à (0.85, 0.85) puis à (0.85, 0.72) est dans le même quadrant nommé dans les deux cas et n'est noté satisfait que dans le premier ; il lira dans la restitution (`summary-view.tsx:96`) une question à laquelle il a répondu oui. C'est le décalage question/règle qui a fait scinder un critère de `hint-budget` | Reformuler la question sur ce que la règle lit, sans nommer le seuil : « Une pratique qu'un garde-fou tient a-t-elle été située là où elle se tient ? » |
| 🟡 | rot | 4/5 | `src/games/practice-map/components/composites/practice-plane.tsx:220` vs `config/course.json` `highRigorFrom: 0.7` | Deux définitions de « haute rigueur » se contredisent sur le même écran. La croix est tracée à 50 %, les quadrants ancrés à leurs coins portent les clés `highRigorLowIntensity` / `highRigorHighIntensity` — la moitié haute est donc nommée « haute rigueur » par le dessin — alors que la notation et l'annonce coupent à 0.7. Scénario : un jeton posé à (0.4, 0.6) est visuellement sous le libellé « Outillé, à la main » et s'entend annoncer « vous le faites, rien ne la vérifie » | Aligner les trois sources : soit `highRigorFrom` à 0.5, soit tracer le trait horizontal au seuil réel, soit renommer les clés de `quadrantsSchema` sur ce qu'elles désignent réellement (les moitiés géométriques du plan) |
| 🟡 | rot | 4 | `__tests__/fixtures/practice-map-answer.ts:22-25` et `aidd_docs/tasks/.../phase-4.md:108` | Le relevé de difficulté annonce `c3` à **6,25 %** au hasard, calculé sous une hypothèse d'indépendance écrite noir sur blanc (« six sur sept **indépendantes** donne `8/128` »). Les sept relations ne sont pas indépendantes : `p1` apparaît dans `o2`, `o5`, `o6` (rigueur) et `o7` (intensité). Valeur réelle sur le corpus, exacte et confirmée par 20 M de tirages : **8,33 %** (`1/12`), soit un tiers de plus qu'annoncé. `c1` (0,033 %) et `c2` (13,28 %) sont justes | Remplacer 6,25 % par 8,3 % aux deux endroits et retirer la mention « indépendantes », qui est fausse pour ce corpus et le resterait pour tout corpus où une pratique porte plus d'une relation |
| 🟡 | fit | 2/4 | `src/games/practice-map/practice-map.evaluator.ts:63-69` + `helpers/read-placements.helper.ts:90` | `c2` lit `inZone && zoneIsHighRigor` — c'est-à-dire exactement ce que `c1` lit, restreint à deux pratiques. Mesuré sur le corpus réel pour un joueur qui pose chaque pratique dans sa zone avec probabilité *p* : `P(c2 | c1)` vaut 89,2 % à *p*=0,3, 91,4 % à 0,5, 95,1 % à 0,7, 99,2 % à 0,9. `c2` n'ajoute donc quasiment aucune information sur `pilotage-contexte` au-delà de `c1`, tout en étant le critère le plus prenable à l'aveugle (47,6 %). Un point sur quatre de la dimension est dépensé à mesurer une seconde fois ce que `c1` mesure déjà | Soit faire porter `c2` sur un axe que `c1` ne lit pas (par exemple : la pratique de haute rigueur est posée plus haut que **toutes** les autres, ce qui est relatif et non absolu), soit l'abandonner et reporter son poids sur `c3` |
| 🟡 | rot | 1 | `src/games/practice-map/helpers/read-placements.helper.ts:9-13` vs `:83` | Le docblock affirme « aucun seuil : les seuils sont déclarés dans le parcours et lus par les règles de l'évaluateur, **jamais ici** — ce helper est aussi lu par l'écran, qui ne doit rien savoir des seuils ». La ligne 83 lit `config.highRigorFrom`. C'est précisément par ce chemin que l'écran en vient à connaître le seuil (constat 🔴 ci-dessus). Le critère d'acceptation `phase-1.md:130` (« le helper ne lit aucun seuil ») est ambigu — il peut viser les seuils de critère (`threshold`) plutôt que ceux de configuration — et devrait être tranché plutôt que deviné | Corriger le docblock pour dire ce que le code fait, ou sortir `zoneIsHighRigor` du helper et le calculer dans l'évaluateur, seul consommateur légitime |
| 🟡 | functional | 5 | `aidd_docs/backlog/defects/practice-map-pousse-la-soumission-hors-de-l-ecran-mobile.md` | Critère `phase-5.md:92` non tenu : l'action primaire reste hors du premier écran aux deux gabarits. **Déjà mesuré, écrit et arbitré** — reporté ici pour que le compte de la revue soit juste, pas comme une découverte | Rien à faire dans cette PR ; le défaut porte la suite |
| 🟢 | rot | 1 | `src/games/practice-map/helpers/read-placements.helper.ts:68-69`, `:98-100` | Deux commentaires annoncent « le `find` ci-dessous rend donc toujours une valeur » alors que le code utilise `Map.get` depuis `:71` et `:102`. Commentaire resté sur le mécanisme d'avant | Écrire `get` |
| 🟢 | code | 3 | `src/games/practice-map/hooks/use-practice-map.hook.ts:218-219` | `parsed.poles as Poles` et `parsed.quadrants as Quadrants` : `parsed` est déjà typé `PracticeMapConfig`, ces deux assertions ne changent aucun type et masqueraient une divergence future | Retirer les deux `as` |
| 🟢 | rot | 5 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-practice-map/phase-5.md:98` | Le bloc « Tenu, mesuré » affirme « Aucune ligne de quadrant, à aucune taille » et « La réserve se plafonne à trois entrées visibles et se replie derrière un `<details>` natif ». Les deux sont faux du code livré ; ils sont annulés plus bas (`:115`, `:136`) mais le bloc reste lisible comme un constat de vérification tenu | Barrer ou annoter le bloc `:98` sur place, comme `:115` le fait pour la contrainte qu'il annule |
| 🟢 | code | 3 | `__tests__/unit/games/practice-map/use-practice-map.test.ts:270-276` | `positionLabel` n'est testé qu'à (0.9, 0.9) et (0.1, 0.1) — jamais autour de la frontière. Aucun test n'aurait détecté le 🔴 ci-dessus, ni ne le détectera après correction | Ajouter un cas qui vérifie que le mot de rigueur ne change pas entre 0.6 et 0.7, c'est-à-dire que l'annonce ne suit pas `highRigorFrom` |
| 🟢 | code | 3 | `src/games/practice-map/components/composites/practice-plane.tsx:154-156`, `:208` | Après un dépôt, `interactive` repasse à `false` et le plan perd `tabIndex=0` sans que le focus soit rendu à un point de départ. Le joueur au clavier retombe sur un conteneur non tabulable et doit retraverser les badges déjà posés (jusqu'à six boutons) pour revenir à la réserve poser la pratique suivante. Le parcours reste praticable, il n'est pas rendu | Rendre le focus à la ligne de légende de la pratique qui vient d'être posée |

## Verification

| Metric        | Value |
| ------------- | ----- |
| Verified      | 82 % (14/17) |
| Files checked | `src/games/practice-map/schema/config.schema.ts`, `schema/answer.schema.ts`, `helpers/read-placements.helper.ts`, `practice-map.evaluator.ts`, `hooks/use-practice-map.hook.ts`, `actions/build-practice-map-answer.action.ts`, `components/composites/practice-map-game.tsx`, `components/composites/practice-plane.tsx`, `components/composites/practice-tray.tsx`, `components/elements/practice-token.tsx`, `components/elements/marker-line.tsx`, `src/games/register-games.ts`, `src/games/register-components.ts`, `config/course.json`, `__tests__/fixtures/practice-map-answer.ts`, `__tests__/unit/games/practice-map/*`, `__tests__/integration/course-run/practice-map-run.test.ts`, `.impeccable/surfaces/ce-map-components-composites-practice-map-game-tsx.md`, `aidd_docs/tasks/2026_08/2026_08_30_jeu-practice-map/{plan,phase-1..5}.md`, `DESIGN.md` |
| Unchecked     | `phase-1.md:130` clause « le helper ne lit aucun seuil » — fix ; `phase-3.md:164` clause « la position atteinte est annoncée en mots » — fix (l'annonce divulgue `highRigorFrom`) ; `phase-5.md:92` clause « la réserve pleine ne pousse pas l'action primaire hors de l'écran » — fix, défaut de backlog déjà ouvert et arbitré. La clause « aucune ligne de quadrant » de `phase-3.md:164` et `phase-5.md:92` est not-applicable : annulée par le chef en `phase-5.md:115` |
| Unplanned     | `polesSchema` et `quadrantsSchema` (`config.schema.ts:22-46`) n'étaient pas au plan ; l'écart est documenté sur place (`:12-19`, `:30-38`) et en `phase-1.md:144`, `phase-4.md:144`. Les douze captures de `qa/` et le `qa/README.md` dépassent la lettre de `phase-5.md:94` (trois temps demandés, cinq états livrés par gabarit) |
