My confidence level of correctness now: 88%

<!-- Challenge de `practice-map` (be70886..0cfbef5) contre plan.md, la story et l'épique. Trois questions du chef : le jeu mesure-t-il la lecture ou l'adresse ; le verdict est-il explicable ; le point vaut-il sa place. Tout chiffre ci-dessous est recalculé sur `config/course.json`, pas repris d'un document. -->

# Correctness (100%)

- **Le jeu mesure la lecture, pas l'adresse à parquer des jetons — sur 3 de ses 4 points.** `c1` (poids 2) et `c3` (poids 1) résistent à toute stratégie aveugle testée : `c1` vaut 0,03 % au hasard et aucune disposition géométrique ne dépasse 1,84 % ; `c3` vaut 8,33 % au hasard et la meilleure disposition aveugle rend 8,41 %, soit rien. Un joueur qui ne lit pas ne peut pas les tenir.
- **La discrimination est réelle et monotone.** Pour un joueur qui pose chaque pratique dans sa zone avec probabilité *p*, le score du jeu vaut 31 % à *p*=0,3, 50 % à 0,5, 86 % à 0,7, 98 % à 0,9. L'instrument sépare les niveaux de compétence sur toute sa plage.
- **Aucune barrière motrice.** Les zones font 3,6 à 7,5 % du plan, soit 0,2 à 0,3 d'amplitude par axe : deux à trois crans clavier, 50 à 80 px sur le plan desktop de 264 px. La cible est large ; le treillis clavier est exact depuis `0cfbef5` (11 valeurs pour 11 positions, chemins convergents, vérifié). Ni la souris ni le clavier ne demandent de précision de geste.
- **L'exploit de `c2` est réel mais inerte sur la signature.** `pilotage-contexte` est une dimension de signature dont les seuils sont `AIDD en route ≥ 0,35` et `AIDD confirmé ≥ 0,60` (`config/signature.json`), et son score vaut `earned / possible` sur 10 unités de poids (`weighted-mapping.strategy.ts:39-47`). Mesuré, en créditant au parqueur les deux unités gratuites du parcours :

  | Stratégie sans lire une ligne | c1 | c2 | c3 | score moyen | P(≥ 0,35) | P(≥ 0,60) |
  | --- | --- | --- | --- | --- | --- | --- |
  | placement au hasard | 0,0 % | 13,2 % | 8,4 % | 0,222 | 2,34 % | 0,02 % |
  | ligne à rigueur 0,80 (l'exploit chiffré) | 0,0 % | 47,6 % | 0,0 % | 0,248 | **0,00 %** | 0,00 % |
  | escalier haut, rigueur 0,70 à 1 | 0,0 % | 41,9 % | 8,3 % | 0,250 | 5,15 % | 0,00 % |
  | diagonale plein cadre | 0,0 % | 28,5 % | 4,1 % | 0,233 | 3,55 % | 0,00 % |

  La stratégie qui maximise `c2` franchit le premier seuil **zéro fois**, et aucune n'atteint jamais le second. Parquer achète 2,8 points de chance sur un seuil, et zéro niveau.
- **La raison est structurelle, et c'est la vraie qualité du corpus : `c2` et `c3` sont géométriquement antagonistes.** Tenir `c2` à l'aveugle exige d'aplatir les sept jetons à une même rigueur haute, ce qui met en égalité les quatre relations d'ordre de rigueur et effondre `c3` à 0 %. Tenir `c3` exige de l'étalement, qui dilue `c2`. Les refus du schéma — zones disjointes, répartition obligatoire de part et d'autre des deux axes — produisent cette tension sans qu'elle ait été nommée dans le plan.
- **La marge de sept pratiques pour un seuil de quatre fait exactement ce qu'elle promettait.** L'axe d'intensité est ambigu entre deux lectures — « qui exécute ce geste » et « à quel point votre flux est délégué quand vous tenez cette pratique ». Le corpus n'en applique pas une seule : `p1` et `p2` suivent la seconde (relire un diff soi-même est posé à 0,35-0,65 alors que son libellé dit « soi-même »), `p4` suit la première (écrire le fichier de contexte est posé à 0-0,25 alors qu'outiller la délégation est la chose la plus déléguante du lot). Un joueur au raisonnement juste qui tranche l'ambiguïté dans un sens ou dans l'autre tient malgré tout les trois critères **dans 100 % des cas** — mesuré : convention A rate `p1` et `p2`, il reste 5 zones sur 7 ; convention B rate `p4`, il en reste 6. Le seuil de quatre absorbe l'incohérence entière.
- **Rien ne fuite.** Les quatorze surfaces lues ou entendues du jeu ont été énumérées une à une : aucune ne se formule sur `expected`, `marker`, `highRigorFrom`, ni sur les seuils `4` et `6`. Le seul canal qui existait est fermé et verrouillé par un test qui échoue sur l'ancien code.
- **Le jeu vaut largement sa place, et plus que ce que le plan revendique : il porte 40 % de toute la dimension `pilotage-contexte`** (4 unités sur 10), et les deux autres contributeurs, `g2-1` et `g2-3`, sont encore les `test-bench` placeholders que l'épique elle-même décrit comme « pesant à plat sur tous les axes ». C'est aujourd'hui la seule mesure réelle de la dimension.
- **La promesse « on comprend pourquoi » n'est pas portée par ce jeu, et c'est écrit.** `BRIEF.md:33` la pose comme critère de jury, `restitution-du-verdict.md:17` l'assigne explicitement à l'épique de restitution : « C'est là que le produit tient ou tombe. » Ce que `practice-map` doit fournir, ce sont des critères attribuables à une dimension — il les fournit, et le sommaire les rend avec leur question (`summary-view.tsx:96`). La révélation à sept repères est un supplément, pas la dette.

# Deal breakers

- Aucun. Les trois réponses de confiance sont positives : le travail livré tient, chaque choix conséquent est soit vérifié soit arbitré par écrit, et le résultat de bout en bout fait ce qu'il annonce. Les points ci-dessous sont des améliorations et des transmissions, jamais des blocages.

# Suggestions (enhancements only)

- **Le bruit de `pilotage-contexte` n'est pas dans ce jeu, il est à côté — et il est plus gros.** `test-bench` n'a aucun garde-fou de soumission (`test-bench-game.tsx:52`, un `Button` sans `disabled`), et `no-unexpected-selected` est vrai à vide (`test-bench.evaluator.ts:57-60`, `every` sur un ensemble vide). Un joueur qui ne coche rien sur `g2-1` et `g2-3` encaisse **2 unités sur 10 de la dimension**, sans lire, avec certitude — contre 1 unité une fois sur deux pour l'exploit de `practice-map`. Si un seul chantier anti-triche doit être ouvert après cette PR, c'est celui-là, pas `c2`. Hors périmètre de ce diff ; à porter en défaut de backlog.
- **Le jeu jette l'information dont la restitution aura besoin.** `restitution-du-verdict.md:35` exige qu'une personne « sache nommer l'action qui la ferait monter ». Pour un `c1` manqué, elle ne le peut pas : elle ignore laquelle des sept pratiques elle a mal située. L'information existe pourtant au moment du verdict — `readPlacements` calcule un `PlacementReading` par pratique avec son `inZone` — mais le port `GameEvaluator` ne rend que `{criterionId, satisfied}` (`game-evaluator.interface.ts:9-12`) et tout le reste est perdu. Ce n'est pas à corriger dans cette PR : le port est partagé par les huit jeux et l'élargir est une décision d'architecture. C'est à savoir maintenant, parce que la restitution héritera d'un `c1` manqué qu'elle ne pourra pas expliquer avec les données qu'on lui passe.
- **Ajouter à la révélation le compte, qui ne fuite rien.** « Vous en avez situé N sur sept là où elles se tiennent » ne donne aucune information de position — donc ne compromet pas le rejeu, contrairement à l'affichage des zones que le plan refuse à juste titre — et transforme sept repères indifférenciés en une mesure que le joueur peut relier à son geste. Aujourd'hui un joueur à 3 sur 7 lit sept repères dont quatre le concernent, sans pouvoir dire lesquels : la moitié de la révélation est du bruit pour lui.
- **Trancher la convention de l'axe d'intensité dans le corpus, ou dans les pôles.** L'incohérence `p1`/`p2` contre `p4` est absorbée par la marge aujourd'hui, mais elle repose sur elle : elle coûte une à deux zones à un joueur dont le raisonnement est juste. Soit les pôles disent laquelle des deux lectures s'applique — `intensityLow` deviendrait « le travail reste chez vous » plutôt que « vous le faites », qui décrit le geste et non le flux —, soit `p4` rejoint la convention de `p1` et `p2`. Le second choix rouvrirait les refus de répartition du schéma ; le premier ne coûte que deux chaînes de corpus.
- **Nommer l'antagonisme `c2`/`c3` dans la table `Decisions`.** C'est ce qui rend l'exploit inerte, et rien ne l'écrit : un futur ajusteur de corpus qui resserrerait `c3` de 6 à 5, ou qui rapprocherait les zones de `p3` et `p4` d'une même rigueur, casserait la propriété sans savoir qu'elle existait. Une ligne suffit à la rendre défendable.
- **Trois verts de la revue 2 restent ouverts**, sans conséquence de mesure : les commentaires `find`/`get` de `read-placements.helper.ts:77` et `:107`, le bloc « Tenu, mesuré » de `phase-5.md:98` qui affirme deux choses fausses du code livré, et le focus perdu après un dépôt au clavier (`practice-plane.tsx:154-156`).
