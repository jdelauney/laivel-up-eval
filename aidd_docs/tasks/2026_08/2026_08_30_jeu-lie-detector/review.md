# Review: Le jeu `lie-detector`, désigner la menteuse puis tenir sa ligne

- **Verdict**: blocked
- **Diff**: `main...dca1618`
- **Axes run**: code, functional, relevancy
- **Date**: 2026_08_30
- **Findings**: 2 critical, 4 warning, 5 minor

## Phases

### Phase 1 — Les contrats et la lecture pure des manches

- [x] Une configuration dont toutes les objections pointent une affirmation vraie est refusée au chargement, en nommant le corpus — `src/games/lie-detector/schema/config.schema.ts:125-136`, testé `__tests__/unit/games/lie-detector/config.schema.test.ts:119-138` (les deux natures, `path: ['rounds']`)
- [x] Une manche à zéro ou deux menteuses est refusée, en nommant la manche — `config.schema.ts:97-113`, testé `config.schema.test.ts:78-107`
- [x] Une trace qui omet une manche, ou qui désigne une affirmation absente du lot, lève l'erreur nommée qui porte l'identifiant fautif — `src/games/lie-detector/schema/answer.schema.ts:108,116-121`, testé `answer.schema.test.ts:59,85`
- [x] La lecture d'une manche où le joueur a désigné exactement la cible de l'objection rend `contradicted: false` et `capitulated: false` — `src/games/lie-detector/helpers/read-rounds.helper.ts:44,46-47`, testé `read-rounds.test.ts:99`
- [x] La lecture d'une manche où une désignation juste est abandonnée sous contradiction rend `capitulated: true` et `unmasked: false` — `read-rounds.helper.ts:46-47`, testé `read-rounds.test.ts:70`
- [x] La lecture d'une manche où une désignation fausse est corrigée vers la menteuse rend `capitulated: false` et `unmasked: true` — `read-rounds.test.ts:85`
- [x] `npm run lint`, `npm run typecheck` et `npm run test` passent — rejoués : `biome check .` 180 fichiers sans problème, `tsc -b --noEmit` muet, 65 fichiers / 560 tests au vert

### Phase 2 — L'évaluateur et ses deux règles

- [x] Une trace de quatre manches démasquées sans capitulation satisfait les deux critères — `__tests__/unit/games/lie-detector/evaluator.test.ts:83-95`
- [x] Une trace dont aucune manche n'a été contredite fait ressortir `no-capitulation` manqué — `src/games/lie-detector/lie-detector.evaluator.ts:56-59`, testé `evaluator.test.ts:120-131` (corpus synthétique ; jamais sur le corpus réel, cf. F3)
- [x] Une règle absente du jeu lève `UnknownRuleError` en nommant la règle et le jeu — `lie-detector.evaluator.ts:30-35`, testé `evaluator.test.ts:133-147`
- [x] Le registre résout `lie-detector` vers son évaluateur et ses deux schémas — `src/games/register-games.ts:59-63`, testé `evaluator.test.ts:75-81` (l'`answerSchema` du contrat résolu n'est pas asserté)
- [x] `npm run lint`, `npm run typecheck` et `npm run test` passent — rejoués

### Phase 3 — Le jeu à l'écran, sa désignation et son objection

- [x] La trace soumise porte une entrée par manche, dans l'ordre de la configuration — `src/games/lie-detector/actions/build-lie-detector-answer.action.ts:29-35`, testé `build-answer.test.ts:40`, `lie-detector-game.test.tsx:131-135`
- [x] Cliquer une seconde affirmation en phase `picking` ne déplace pas la désignation — tenu par l'absence de chemin : `use-lie-detector.hook.ts:65-69` bascule la phase au premier clic, testé `use-lie-detector.test.ts:62-77`
- [x] Après le second geste, aucune fonction exposée ne change plus la désignation de la manche — `use-lie-detector.hook.ts:64-81`, testé `use-lie-detector.test.ts:106-123`
- [x] Deux appels à `advance()` sur la dernière manche ne soumettent qu'une fois — `use-lie-detector.hook.ts:109-111`, testé `use-lie-detector.test.ts:163-203`
- [x] Avant la révélation, le texte rendu ne contient aucune vérification et ne nomme pas la menteuse — tenu par construction (`use-lie-detector.hook.ts:114-120,149-157` n'expose ni `lying` ni `verification`), testé `lie-detector-game.test.tsx:96-104` et `use-lie-detector.test.ts:46-60` (assertions plus étroites que leur titre, cf. F8)
- [x] L'état d'une affirmation se lit sans la couleur : un signe et un libellé le portent — `src/games/lie-detector/components/elements/claim-card.tsx:46-80,86-92`, testé `lie-detector-game.test.tsx:177-185`
- [x] Le parcours résout `lie-detector` vers son composant — `src/games/register-components.ts:24` (aucun test n'assert `resolveGameComponent` ; aucun jeu du dépôt n'en a)
- [x] `npm run lint`, `npm run typecheck` et `npm run test` passent — rejoués

### Phase 4 — Le jeu dans le parcours, et son corpus

- [x] Le parcours réel se charge sans refus, avec `g1-3` en `lie-detector` — `config/course.json` g1-3, testé `__tests__/integration/course-run/lie-detector-run.test.ts:140-147`
- [x] Aucun critère de `g1-3` ne mappe un axe du référentiel officiel — `lie-detector-run.test.ts:149-158` (`intervention` retiré des deux critères)
- [x] Le profil qui adopte la cible de chaque objection ressort sous le seuil d'identification — `lie-detector-run.test.ts:175-184` ; mesuré sur le corpus réel : 1 manche démasquée sur 4 contre un seuil de 3
- [ ] Le profil qui tient chacune de ses **désignations justes** satisfait le critère de stabilité — le fixture `neverMovesPicks` (`lie-detector-run.test.ts:113-119`) construit ses désignations avec `nonLiarIdOf`, donc **fausses dans les quatre manches** ; le test assert `satisfied === true` sur ce profil. Le critère tel qu'écrit n'est pas couvert, et le test verrouille F1 au lieu de la stabilité. → F2
- [x] Le profil juste puis retourné à chaque objection rate les deux critères — `lie-detector-run.test.ts:197-210` ; mesuré : 1 démasquée, 3 capitulations
- [x] Un corpus réécrit avec des objections d'une seule nature fait échouer le test, pas seulement le schéma — `lie-detector-run.test.ts:212-224` ; corpus réel : r2 fondée, r1/r3/r4 creuses
- [x] Dans chaque manche, la menteuse n'est ni la plus longue ni la plus courte, et le lot ne s'étale pas de plus d'un quart — `lie-detector-run.test.ts:226-255`
- [x] `npm run lint`, `npm run typecheck` et `npm run test` passent — rejoués

### Phase 5 — La passe impeccable de la surface

- [x] La fiche de surface existe et nomme la stratégie retenue pour ce jeu — `.impeccable/surfaces/…lie-detector-game-tsx.md`, « La grille de comparaison »
- [ ] Les quatre affirmations d'une manche se comparent sans défilement au premier temps — asserté par construction (`round-sheet.tsx:62`, `sm:grid-cols-2`) et jamais vérifié au rendu réel ; aucune tournée navigateur, aucun dossier `qa/`, là où les quatre jeux précédents en ont un. → F5
- [x] Une objection fondée et une objection creuse sont rendues avec exactement la même structure et le même ton — `objection-note.tsx:18` ne reçoit que `argument`, testé `lie-detector-game.test.tsx:193-220`
- [x] Un test échoue si la présentation de l'objection dépend de sa nature — `lie-detector-game.test.tsx:193-220` (portée limitée au sous-arbre `ObjectionNote`, cf. F8)
- [x] `npm run lint`, `npm run typecheck` et `npm run test` passent — rejoués

## Findings

| Sev | Kind | Phase | Location | Issue | Fix |
| --- | ---- | ----- | -------- | ----- | --- |
| 🔴 | fit | 2 | `src/games/lie-detector/lie-detector.evaluator.ts:56-59` | `no-capitulation` mesure la mauvaise matière. La garde est `contradictedCount > 0 && capitulationCount === 0`, or `contradicted` (`read-rounds.helper.ts:44`) vaut « la cible de l'objection diffère de la première désignation » — vrai pour tout joueur qui désigne mal. Une capitulation exige `firstPickId === liarId` (`:46-47`) : un joueur qui ne désigne jamais la menteuse est contredit **et** ne peut jamais capituler, donc décroche le critère. Mesuré sur le corpus réel : le suiveur pur (aucune lecture) sort à 1 manche démasquée sur 4, contredit 4 fois, **0 occasion réelle de capituler**, `c2` **satisfait** ; le profil qui ne bouge jamais et lit faux, idem. Deux points de poids sur `verification` récoltés sans lire un mot. La story refuse explicitement la satisfaction par vacuité (`demasquer-l-affirmation-qui-ment.md:56`, jurisprudence `kinds-found-including`) : la garde livrée compte les contradictions, jamais les occasions | Compter les occasions, pas les contradictions : une manche porte matière quand `contradicted && firstPickId === liarId`. Règle : `opportunityCount > 0 && capitulationCount === 0`. Non régressif, vérifié sur le corpus réel : le profil visé (démasque 4/4 et tient) garde 3 occasions et 0 capitulation, donc reste satisfait |
| 🔴 | code | 4 | `__tests__/integration/course-run/lie-detector-run.test.ts:113-119` et `:186-195` | Le test entérine F1 comme comportement voulu. `neverMovesPicks` dérive ses désignations de `nonLiarIdOf(round)` — l'affirmation **vraie**, donc un mauvais choix dans les quatre manches — et le test assert `stability?.satisfied === true`. L'acceptance de phase 4 dit « le profil qui tient chacune de ses désignations **justes** » : le fixture ne tient que des désignations fausses. Le critère écrit n'est couvert par aucun test | Construire le profil depuis `liarIdOf` pour coller à l'acceptance, et ajouter un profil qui ne désigne jamais la menteuse en assertant `g1-3-c2` **manqué** |
| 🟡 | code | 4 | `__tests__/integration/course-run/lie-detector-run.test.ts:175-184` | Le refus de la vacuité n'est jamais asserté sur le corpus réel. `alwaysFollowsAssistantPicks` pose première et dernière désignation sur `objection.targetId`, donc `contradictedCount === 0` : c'est exactement le joueur jamais contredit que la story oblige à rater `g1-3-c2`. Le test n'assert que `g1-3-c1`. La preuve la plus forte disponible est à une assertion, et n'est pas prise. Couverture unitaire existante (`evaluator.test.ts:120-131`) mais sur corpus synthétique. Note : ce profil est injouable à l'écran (l'objection est cachée en phase `picking`), il modélise une trace forgée, pas un joueur | Ajouter `expect(satisfiedByCriterion['g1-3-c2']).toBe(false)` dans ce test |
| 🟡 | rot | 3 | `src/games/lie-detector/hooks/use-lie-detector.hook.ts:134-147,172` | `currentReading` est une surface morte. Le `useMemo` reconstruit une configuration et une trace réduites à la manche courante pour rappeler `readRounds`, le hook la retourne, et **aucun composant ne la consomme** : `lie-detector-game.tsx:24-38` ne la déstructure pas. Seul son propre test la lit (`use-lie-detector.test.ts:42,135`). Elle existe pour alimenter l'encart (8) du wireframe de phase 3 — « Le relevé de la manche : ce que vous avez désigné, puis tenu » — jamais construit et jamais rétracté : ni phase 5 ni la fiche de surface ne mentionnent un relevé | Rendre le relevé que le plan projette, ou supprimer `currentReading` et ses 14 lignes puis acter l'abandon dans `phase-3.md` |
| 🟡 | conform | 5 | `.impeccable/surfaces/…lie-detector-game-tsx.md` (section « Vérifié »), `aidd_docs/tasks/2026_08/2026_08_30_jeu-lie-detector/phase-5.md:11-23,105-113` | Aucune tournée navigateur, contre la barre posée par les quatre jeux précédents. `checkpoints`, `three-tracks`, `confidence-bet` et `defect-hunt` portent chacun `aidd_docs/tasks/…/qa/` avec captures 1440×900 et 390×844 ; `defect-hunt/phase-5.md:148,163` portait même la tâche et la ligne d'acceptance « La tournée aux deux gabarits est déposée dans `qa/` ». Ici phase 5 déclare 7 scénarios `browser` puis ne projette aucun `qa/` et n'en fait aucune acceptance. La fiche justifie l'absence par « le projet n'a pas encore de harnais Playwright amorcé » — exact sur la suite automatisée (`aidd_docs/memory/testing.md:7`) mais hors sujet : les quatre tournées précédentes ont eu lieu sans elle, et les deux dernières ont chacune trouvé des défauts de mise en page réels | Jouer la tournée aux deux gabarits, la déposer dans `qa/`, et y adosser l'acceptance « les quatre affirmations se comparent sans défilement » |
| 🟡 | fit | 4 | `__tests__/integration/course-run/lie-detector-run.test.ts:140-158` | Le seuil de `g1-3-c1` n'est épinglé par rien. La tâche `2.2` de phase 4 demande de vérifier « type, nombre de manches, **seuil du premier critère**, absence de mapping hors `verification` » : les trois premiers points sont assertés, le seuil non. La story le fixe à « au moins 3 manches sur 4 ». Les profils existants ne le bornent qu'à 2..4 — un seuil de 1 casserait le test du suiveur, mais 2 comme 4 passent en silence — donc une dérive hors de 3 sort au vert | Asserter `rule.threshold === 3` sur `g1-3-c1` |
| 🟢 | rot | 2 | `src/games/lie-detector/lie-detector.evaluator.ts:74-79,103-108` | `VerdictInputs.config` n'est jamais lu : les deux règles ne prennent que des décomptes (`:90,92-95`). Le commentaire du type dit « Tout ce qu'une règle peut lire, et rien de plus » et la tâche `1.7` de phase 2 dit « les lectures que **les règles consomment** » : le champ contredit les deux. Recopié de `src/games/defect-hunt/defect-hunt.evaluator.ts:134`, où `config` est bien lu par `within-time-budget`. `noUnusedLocals` ne voit pas une propriété d'objet inutilisée | Retirer `config` du type et du littéral |
| 🟢 | code | 3 | `__tests__/unit/games/lie-detector/lie-detector-game.test.tsx:96-104`, `:193-220`, `use-lie-detector.test.ts:46-60` | Les gardes anti-fuite sont plus étroites que leurs titres. (a) « leaks nothing before the reveal » ne vérifie l'absence que d'**une** vérification sur quatre, plus le mot `/menteuse/i` ; (b) la comparaison fondée/creuse ne porte que sur le sous-arbre `ObjectionNote` (`label.parentElement`) : une passe future qui marquerait la cible de l'objection dans la grille selon sa nature passerait au vert ; (c) la garde de fuite du hook ne tourne qu'en phase `picking`, jamais rejouée après `designate` | Boucler sur les quatre vérifications ; comparer `container.innerHTML` entier entre les deux natures ; rejouer la sérialisation du hook après `designate` |
| 🟢 | rot | 3 | `aidd_docs/tasks/2026_08/2026_08_30_jeu-lie-detector/phase-3.md:114,165,166` | Le plan ne suit plus le code. §3.1 spécifie encore un état de `ClaimCard` « pointée par l'assistant » : il n'existe pas, et `round-sheet.tsx:63-86` ne descend jamais `objection.targetId`. §3.2 dit encore que `objection-note.tsx` porte « sa cible et son argument » : il ne prend que `argument`. L'encart (8) du wireframe, « le relevé de la manche », n'est pas rendu. Les trois révisions sont délibérées et argumentées dans la fiche de surface, mais phase 3 n'a jamais été reprise — la règle permanente du projet est que les phases suivent le code à chaque lot livré | Réconcilier `phase-3.md` §3.1, §3.2 et l'encart (8) avec ce qui est livré |
| 🟢 | fit | 4 | `config/course.json` — manche `r3`, affirmation `r3-c` | Affirmation vraie défendable plutôt qu'indiscutable. La story (`demasquer-l-affirmation-qui-ment.md:80`) exige « vraie sans discussion, pas seulement défendable ». « Une dépendance non satisfaite entre deux lots fait échouer le second, **quel que soit le soin mis à le formuler** » est un absolu qu'un assistant disposant du dépôt entier défait — précisément la contre-attaque que porte l'objection creuse de `r3`, et elle tient. Sa `verification` (« il échoue sur ce qui n'existe pas encore ») rejoue le point contesté au lieu de le trancher. La manche reste lisible car `r3-b` est franchement fausse, mais c'est le seul lot du corpus où un joueur attentif peut hésiter entre deux | Ramener `r3-c` à ce qui est indiscutable, ou la remplacer |
| 🟢 | rot | - | `__tests__/integration/course-run/checkpoints-run.test.ts:114-130`, `three-tracks-run.test.ts:111-127` | Duplication qui croît avec chaque jeu. Le bloc `if (game.type === 'lie-detector')` est recopié à l'identique dans les deux fichiers, seul le commentaire diffère ; c'est la sixième branche de ce genre par fichier. Les deux patchs sont légitimes et nécessaires (g1-3 a changé de type, le repli `{ selected: [] }` serait refusé par l'`answerSchema` du registre), mais le fixture porte désormais six branches quasi identiques, deux fois | Extraire un `answerFor(game)` partagé sous `__tests__/fixtures/` |

## Verification

| Metric        | Value                                             |
| ------------- | ------------------------------------------------- |
| Verified      | 94% (31/33)                                       |
| Files checked | `src/games/lie-detector/**` (10 fichiers), `src/games/register-games.ts`, `src/games/register-components.ts`, `config/course.json`, `__tests__/unit/games/lie-detector/**` (7 fichiers), `__tests__/integration/course-run/lie-detector-run.test.ts`, `__tests__/integration/course-run/checkpoints-run.test.ts`, `__tests__/integration/course-run/three-tracks-run.test.ts`, `.impeccable/surfaces/…lie-detector-game-tsx.md`, `aidd_docs/backlog/stories/demasquer-l-affirmation-qui-ment.md`, `aidd_docs/tasks/2026_08/2026_08_30_jeu-lie-detector/plan.md` + phases 1-5 |
| Unchecked     | Phase 4 « Le profil qui tient chacune de ses désignations justes satisfait le critère de stabilité » — fix (F2) ; Phase 5 « Les quatre affirmations d'une manche se comparent sans défilement au premier temps » — fix (F5) |
| Unplanned     | `__tests__/integration/course-run/checkpoints-run.test.ts:114-130` et `three-tracks-run.test.ts:111-127` — branches `lie-detector` ajoutées à deux fixtures que la projection de phase 4 ne listait pas. Jugées légitimes et nécessaires : `g1-3` passant de `test-bench` à `lie-detector`, le repli `{ selected: [] }` de ces deux parcours serait refusé par l'`answerSchema` du registre et casserait deux suites sans rapport. Réserve de forme seulement (duplication, F11) |
